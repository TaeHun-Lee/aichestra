# Dashboard API-backed Read Model Migration v0

## What Changed

Dashboard API-backed Read Model Migration v0 adds a read-only dashboard data layer:

- shared dashboard read-model DTO types in `packages/shared`;
- scope-aware dashboard/readiness DTOs: `ScopedReadModelMetadata`, `DashboardPanelScopeSummary`, and `ReadinessEndpointScopeSummary`;
- sanitized API read-model assembly in `apps/api/src/dashboard-read-model.ts`;
- `/dashboard/*` API endpoints for overview, tasks, Git, conflicts, registry, registry artifact trust metadata inside the registry panel, LLM, agents, policy, auth/RBAC, scope model readiness, Dashboard/Readiness Tenant Scope Planning, Tenant Scope Enforcement, Audit Query Scope Enforcement metadata in observability, providers, security, Local Agent, MCP Gateway, production readiness, GitHub App hardening and controlled runtime status, observability, database operations, secret backend migration, production secret backend decision, Vault secret backend status, staging deployment readiness, staging deployment dry-run readiness, staging release candidate readiness, staging deployment execution readiness, and audit sections;
- a web `DashboardDataProvider` abstraction with API and demo providers;
- dashboard rendering that consumes read models instead of directly constructing services;
- deterministic tests for API read models, provider mapping, fallback behavior, rendering, and no-secret exposure.

## Read Model Architecture

The API read-model assembler composes current service and repository state only. It does not create tasks, run workflows, evaluate new policy decisions, request secret leases, execute agent runs, create Local Agent fixture invocations, call GitHub, or call LLM providers.

The web layer consumes `DashboardReadModels` through:

- `ApiDashboardDataProvider`: fetches `/dashboard/*` endpoints.
- `DemoDashboardDataProvider`: builds deterministic fixture read models from the existing demo data path for static tests/offline rendering.

Runtime API mode can be enabled with:

```bash
AICHESTRA_DASHBOARD_DATA_SOURCE=api
AICHESTRA_DASHBOARD_API_BASE_URL=http://127.0.0.1:3000
```

If API mode is configured and the API is unavailable, demo fallback is used unless:

```bash
AICHESTRA_DASHBOARD_DISABLE_DEMO_FALLBACK=true
```

## API Endpoints

- `GET /dashboard/overview`
- `GET /dashboard/tasks`
- `GET /dashboard/git`
- `GET /dashboard/conflicts`
- `GET /dashboard/registry`
- `GET /dashboard/llm`
- `GET /dashboard/llm-integration`
- `GET /dashboard/agents`
- `GET /dashboard/policy`
- `GET /dashboard/auth`
- `GET /dashboard/auth-production`
- `GET /dashboard/scopes`
- `GET /dashboard/tenant-scope`
- `GET /dashboard/tenant-enforcement`
- `GET /dashboard/scope-filter`
- `GET /dashboard/providers`
- `GET /dashboard/security`
- `GET /dashboard/local-agents`
- `GET /dashboard/mcp`
- `GET /dashboard/readiness`
- `GET /dashboard/github-app`
- `GET /dashboard/github-app-integration`
- `GET /dashboard/merge-queue-integration`
- `GET /dashboard/git-cleanup`
- `GET /dashboard/registry-compatibility`
- `GET /dashboard/registry-drift`
- `GET /dashboard/registry-canary-apply`
- `GET /dashboard/policy-bundles`
- `GET /dashboard/observability`
- `GET /dashboard/database`
- `GET /dashboard/secret-backend`
- `GET /dashboard/secret-backend-decision`
- `GET /dashboard/vault-secret-backend`
- `GET /dashboard/staging`
- `GET /dashboard/staging-dry-run`
- `GET /dashboard/staging-rc`
- `GET /dashboard/staging-execution`
- `GET /dashboard/ci-cd`
- `GET /dashboard/audit`

Dashboard routes are read-only. Non-GET requests return `method_not_allowed`.

## Dashboard Sections

v0 exposes API-backed read models for:

- Task and TaskRun summaries;
- Real Git Adapter v2 gate/config/state/audit/webhook summaries;
- Multi-user / Multi-session Branch Orchestrator v2 branch requests, decisions, ownership records, naming policy, collision blockers, same-workspace blockers, base drift statuses, merge readiness counts, and no-destructive-Git status under `/dashboard/git`;
- Conflict Manager leases, risks, queue, and simulations;
- Conflict Resolution Assistant v1 requests, summaries, review-only plans, recommendations, suggested validation, merge queue linkage, `applyAllowed: false`, `realLlmUsed: false`, and no-source-mutation/no-merge-execution status under `/dashboard/conflicts`;
- Registry counts, entries, approval queue, packages, revisions, eval results, and improvement/governance summaries with migrated request/correlation metadata where source audit records include it;
- Registry Tenant Scope Enforcement v1 summary metadata under the Registry panel: in-scope/warning/denied/missing counts, approval queue scope summary, resolver scope status, mutation scope status, `productionEnforcement: false`, and resolver gate preservation status;
- Registry Signed Package / Artifact Trust v1 metadata under the Registry panel: artifact digest/signature/provenance visibility, trust summary, trust decisions, unsigned/missing-provenance warnings, digest mismatch blockers, `realSigningImplemented: false`, `realVerificationImplemented: false`, signing keys generated false, no external registry calls, and resolver/checksum gate preservation status;
- Eval Suite Execution Harness v1 metadata under the Registry panel: suite/case/run/verdict counts, deterministic mock execution mode, target/proposal linkage, apply/canary readiness relationship, `externalEvalImplemented: false`, `realProviderCalls: false`, `canaryExecuted: false`, `autoApplyEnabled: false`, and no-secret/no-env status;
- LLM Gateway v2 config, remote gate state, models, routes, provider health, fallback policies, routing decisions, virtual keys, usage, audit, and blocked examples;
- Agent Runner config, runs, command results, workspaces, and audit;
- Agent Workspace Lifecycle v2 leases, lifecycle events, cleanup decisions, isolation status, branch lease linkage, no-destructive-cleanup status, and future worktree execution disabled status;
- Agent Worktree Allocation v1 requests, results, safety checks, allowlist count, branch/workspace lease linkage, `allocationEnabled: false`, `productionWorktreeAllocation: false`, `realGitWorktreeExecuted: false`, and no-destructive-cleanup/no-full-path status;
- Multi-session Agent Run Coordination v1 active sessions, coordination groups, concurrency policies, overlap warnings, same-workspace blockers, same-file warnings, recommended actions, review/merge readiness counts, and no-destructive-action status;
- Cross-session File Lease / Edit Intent Graph v1 edit intents, file leases, graph node/edge counts, overlap assessments, same-file warnings, same-directory warnings, same-workspace blockers, recommendations, and no-file-lock/no-source-mutation status;
- Policy rules, audit entries, and blocked examples;
- Production Auth/RBAC Planning v0 mock auth mode, current actor, roles, teams, service accounts, authorization examples, and auth audit events;
- Production Auth/RBAC v1 Planning provider options, production auth disabled status, mock actor warning, service account readiness, tenant/scope readiness, request context warning, role matrix summary, blockers, and no-token/no-session status;
- Tenant/Repo/Provider Scope Model v1 mock tenant/team/project, repo, provider, model, SecretRef, MCP tool, registry package, Local Agent host, audit query, enforcement-future, and no-secret/no-env summaries;
- Dashboard/Readiness Tenant Scope Planning v1 dashboard panel inventory, readiness endpoint inventory, role visibility matrix, fallback policy, secret-adjacent count, audit-scoped count, production blocker count, and tenant filtering implemented false status;
- Dashboard/Readiness Tenant Scope Implementation v1 section-level scope metadata, missing-scope warnings, role visibility hints, redaction labels, `tenantFilteringImplemented: false`, and `productionEnforcementImplemented: false`;
- Tenant Scope Enforcement v1 partial representative metadata, enforcement modes, mismatch catalog, scope-decision summaries, audit-query warnings, secret-adjacent warnings, tenant filtering false, and production tenant enforcement false;
- Audit Query Scope Enforcement v1 partial representative observability metadata, audit detail-level decisions, redaction plans, missing-scope warnings, raw-payload-forbidden status, production audit storage enforcement false, and no-secret/no-env status;
- Enterprise provider catalog, Local CLI Provider Templates v1, compatibility rules, parser profiles, security constraints, local-agent descriptors, readiness, and audit;
- Secrets/Sandbox metadata, SecretRef-backed GitHub token/webhook and LLM credential status, network/redaction policies, credential audit, and security audit;
- Local Agent Protocol v1 registration, channels, handshakes, compatibility, consent, invocations, streams, and audit;
- MCP Gateway v0 mock server/tool catalog, risk summary, recent invocations, blocked examples, integration safety status, and MCP audit events;
- Production Deployment Readiness Planning v0 deployment profile, blocker, risk, and warning summaries;
- GitHub App / Production Webhook Hardening Planning v0 permission matrix, webhook allowlist, replay/dead-letter, credential, endpoint, risk, and no-secret summaries, plus GitHub App Controlled Implementation v1 runtime config, installation, repository grant, token readiness, and sanitized audit metadata;
- GitHub App integration-test profile v1 live-test gate counts, unsafe gate warnings, test-case status, cleanup policy, and no-secret/no-env summaries;
- Observability / Audit Retention v0 normalized audit, retention/redaction, metric, trace, source coverage, blocker summaries, and Audit Query Scope Enforcement v1 redaction/check metadata;
- Persistent DB Production Operations v1 deployment profile, migration readiness, index review, retention/audit growth, webhook persistence, backup/restore, pooling, risk, and no-DB-url summaries;
- Secret Backend Migration Planning v0 backend option, migration phase, readiness check, risk, rotation plan, lease policy, env fallback warning, credential kind status, blocker, and no-secret/no-env summaries;
- Production Secret Backend Implementation Option Decision v0 recommended backend, second-choice backend, decision criteria, backend scores, implementation scope, provider mapping, risk summary, env fallback warning, and no-secret/no-env summaries;
- Vault-backed Secret Backend v1 selected backend status, Vault enabled/configured booleans, client health, readiness checks, SecretRef examples, blocked examples, audit summaries, env fallback warning, and no-secret/no-token summaries;
- Vault Integration-Test Profile v1 profile status, live-test skip status, missing/unsafe gate counts, Vault backend/provider/address/token configured booleans, path allowlist count, test path allowlisted/test-only booleans, test cases, safety checks, operation policy, and no-secret/no-env summaries;
- Merge Queue Live Integration-Test Profile v1 profile status, live-test skip status, missing/unsafe gate counts, repo allowlist count, branch prefix match status, test repo allowlist status, test source branch counts/match-prefix booleans, dry-run-only status, no-auto-merge/no-force-push/no-branch-delete status, cleanup policy, test cases, safety checks, and no-secret/no-env/no-repo-url/no-branch-name summaries;
- Branch Cleanup / Orphan Lease Recovery v1 scan counts, orphan record severity buckets (critical/high), recommendation counts (metadata-only vs destructive-future), decisions, recovery actions, policy status (scan/recommend/decide/metadata-execute allowed; destructive cleanup/branch deletion/worktree removal/PR closure denied), and no-destructive status (destructive cleanup disabled, no real branch deletion / worktree removal / PR closure / remote Git / filesystem deletion, no secrets/env values exposed);
- Staging Deployment Profile v0 profile status, integration gates, readiness checks, promotion criteria, rollback criteria, blockers, warnings, mock actor warning, env fallback warning, and no-secret/no-env summaries;
- Staging Deployment Dry-run Profile v0 overall status, source summaries, required checks, critical blockers, skipped integration profiles, recommended next actions, promotion/rollback guidance, and no-secret/no-env summaries;
- Staging Release Candidate Checklist v0 overall status, required gates, blockers, skipped optional tests, signoff status, release-note requirements, rollback checklist, recommended next actions, and no-release/no-deployment/no-secret/no-env summaries;
- Staging Deployment Execution Plan v0 plan status, step sequence, pre-deploy gates, optional live integration decisions, go/no-go decision, human signoff pack pending status/counts, pending signoffs, rollback plan, recommended next actions, and no-release/no-deployment/no-secret/no-env summaries;
- Staging CI/CD Pipeline Planning v0 profile status, job matrix, required baseline jobs, optional integration-test gates, secret/env safety status, artifact/report policy, staging promotion, cleanup/rollback, risks, blockers, Node/Volta status, and no-secret/no-env summaries;
- aggregated audit groups.

## Safety Rules

- No GitHub calls.
- No LLM provider calls.
- No vendor CLI execution.
- No runner command execution.
- No workspace deletion, real Git worktree creation/removal, branch checkout/switch, merge, rebase, fetch, or push from dashboard read routes.
- No workflow execution.
- No secret lease issuance.
- No MCP real transport or tool invocation from dashboard read routes.
- No credential cache reads.
- No production DB connection, migration execution, backup/restore job, destructive retention job, or database URL exposure.
- No real secret backend connection, secret migration, rotation job, credential issuance, or env value exposure.
- No production secret backend decision panel backend call, secret read, secret migration, rotation job, production credential issuance, credential cache read, BYOK/OAuth/WIF/IAM exchange, or production-ready claim.
- No Vault secret backend panel live call in default runtime/tests, Vault token exposure, secret value exposure, env value exposure, broad path listing, secret migration, rotation, production credential issuance, or production-ready claim.
- No staging deployment, infrastructure manifest apply, production traffic, external provider call, remote MCP, vendor CLI execution, or staging env value exposure.
- No staging dry-run deployment, CI job execution, remote integration-test execution, external provider call, resource mutation, secret/env exposure, staging-deployed claim, or production-ready claim.
- No staging RC release creation, Git tag creation, GitHub release creation, deployment execution, CI job execution, remote integration-test execution, external provider call, resource mutation, secret/env exposure, staging-deployed claim, or production-ready claim.
- No staging execution deployment, release creation, Git tag creation, deployment command execution, CI job execution, remote integration-test execution, external provider call, resource mutation, credential issuance, secret/env exposure, staging-deployed claim, or production-ready claim.
- No Vault integration-test execution, Vault call, token exposure, env value exposure, raw path/key/value exposure, write/delete/rotate, broad path listing, credential cache read, or production-ready secret-backend claim from dashboard read routes.
- No active CI workflow creation, CI job execution, deployment job, default remote integration test execution, artifact upload, raw CI log upload, or CI env value exposure.
- No GitHub App integration-test profile live execution, env value exposure, private-key read, token exchange, GitHub call, auto-merge, force-push, or branch deletion from dashboard read routes.
- No real auth provider connection, login/logout/session/JWT/password flow, service-account credential issuance, token/cookie/session/assertion exposure, or external IdP call.
- No production tenant filtering, tenant isolation enforcement, row-level security, or production tenancy claim from `/dashboard/scopes`, `/dashboard/tenant-scope`, or `/dashboard/tenant-enforcement`; they are read-only mock/readiness, planning, and partial representative enforcement summaries.
- No dashboard scope metadata entry is an authorization decision; Implementation v1 does not hide panels, enforce filters, or claim production tenant isolation.
- No Tenant Scope Enforcement v1 dashboard metadata overrides Policy-as-code; policy deny remains authoritative and production tenant enforcement remains false.
- No Audit Query Scope Enforcement v1 dashboard metadata implements production audit storage filtering, raw-payload access, external SIEM/export, production Auth/RBAC, sessions/JWTs/API keys, service-account credentials, or tenant isolation; raw payload remains forbidden and production storage enforcement remains false.
- No Registry Tenant Scope Enforcement v1 dashboard metadata filters production registry data or bypasses registry lifecycle, approval, eval, checksum, semver, policy, or governance gates; production registry tenant isolation remains false.
- No Registry Artifact Trust dashboard metadata performs real signing, verifies signatures, generates signing keys, calls artifact registries, exposes raw signatures/secrets/env values, or bypasses registry lifecycle, approval, eval, checksum, semver, policy, governance, tenant scope, or resolver gates.
- No Eval Suite Execution Harness dashboard metadata executes external eval suites, calls real LLM/MCP/providers/vendor CLIs, executes canaries, auto-applies changes, mutates active registry entries through auto-improvement, exposes secrets/env values, or bypasses registry/governance/policy gates.
- No GitHub App private-key read/signing, live installation token exchange, or token/private-key/webhook-secret exposure.
- No raw unredacted logs.
- No tokens, provider API keys, or credential values in API/web output.
- No full local workspace paths from workspace lifecycle DTOs; local paths are redacted and secret-like metadata keys are masked.
- No multi-session coordination endpoint executes agents, mutates workspaces, creates branches, calls providers, or returns secret/env values.
- No Agent Worktree Allocation endpoint runs real `git worktree`, mutates real repositories, deletes workspaces, calls providers, or returns secret/env values/full local paths.
- No branch orchestrator endpoint creates real branches, switches branches, mutates workspaces, calls GitHub, deletes branches, runs merge/rebase/fetch/push, or returns secret/env values.
- No edit-intent endpoint locks files, mutates source files, creates branches/worktrees, calls providers, or returns secret/env values.
- No Conflict Resolution Assistant endpoint applies patches, mutates source files, executes merge/rebase/fetch/push/checkout/switch, releases merge queue holds automatically, calls real LLM providers, calls Git providers, or returns secret/env values.

`sanitizeDashboardValue` masks token-like strings, sensitive env assignments, and credential-cache paths before read models are returned or rendered.

## Fallback Strategy

Demo fallback remains for deterministic static tests and offline local preview. It is explicit in `DemoDashboardDataProvider` and should not be treated as production dashboard state.

API-backed mode is available through `ApiDashboardDataProvider`; it fetches all `/dashboard/*` sections and maps them to the shared read-model DTOs.

## Tests

`tests/dashboard-read-model-v0.test.ts` covers:

- `/dashboard/*` endpoint availability;
- safety flags and blocked examples;
- no-secret exposure;
- auth/RBAC visibility and mock-auth warning;
- read-only behavior for dashboard routes;
- no Git audit mutation from read-model reads;
- database operations panel rendering and no database URL exposure;
- secret backend migration panel rendering and no env/secret value exposure;
- production secret backend decision panel rendering, Vault-first planning visibility, implementation scope, risk summary, and no backend call/env/secret value exposure;
- Vault secret backend panel rendering, config readiness, mock/disabled client status, skipped-live-test status, and no Vault token/secret/env value exposure;
- staging deployment panel rendering and no env/secret value exposure;
- staging deployment dry-run panel rendering, skipped integration profile visibility, health/readiness metadata, and no env/secret value exposure;
- staging release candidate panel rendering, required gates, signoffs, release-note requirements, rollback checklist, skipped optional tests, health/readiness metadata, and no release/deployment/env/secret value exposure;
- staging deployment execution panel rendering, step sequence, gates, go/no-go, human signoff pack pending status/counts, pending signoffs, rollback readiness, health/readiness metadata, and no release/deployment/fake approval/env/secret value exposure;
- staging CI/CD panel rendering and no env/secret value exposure;
- Agent Workspace Lifecycle v2 dashboard/API visibility, workspace path redaction, cleanup decision rendering, no destructive cleanup, and no secret-like metadata exposure;
- Multi-session Agent Run Coordination v1 dashboard/API visibility, session grouping, same-workspace blockers, same-file/same-directory warnings, recommendation rendering, no remote Git, no agent execution, and no secret/env exposure;
- Multi-user / Multi-session Branch Orchestrator v2 dashboard/API visibility, branch naming policy, ownership records, branch collision blockers, same-workspace blockers, base branch drift metadata, merge readiness counts, no destructive Git, and no secret/env exposure;
- PR Ownership / Handoff Model v1 dashboard/API/readiness visibility, owner/reviewer records, handoff requests/decisions, merge queue owner readiness, remote PR update disabled, remote reviewer assignment disabled, GitHub API calls disabled, auto-merge disabled, and no secret/env exposure;
- Cross-session File Lease / Edit Intent Graph v1 dashboard/API visibility, file lease status, edit overlap recommendations, graph rendering, no file locks, no source mutation, no remote Git, and no secret/env exposure;
- Auth/RBAC production readiness panel rendering and no token/cookie/session/assertion exposure;
- Dashboard/Readiness Tenant Scope Planning panel rendering, planning-only status, production enforcement false, tenant filtering implemented false, secret-adjacent count, audit scope warning, and no secret/env exposure;
- Dashboard/Readiness Tenant Scope Implementation panel scope summaries, readiness endpoint summaries, missing-scope warning rendering, role visibility hint rendering, redaction label rendering, `productionEnforcementImplemented: false`, and no secret/env exposure;
- Tenant Scope Enforcement v1 panel rendering, representative-only status, enforcement mode, missing-scope warning, audit-query warning, secret-adjacent warning, `tenantFilteringImplemented: false`, `productionTenantEnforcement: false`, and no secret/env exposure;
- Audit Query Scope Enforcement v1 API/dashboard rendering, role behavior, redaction plans, raw-payload-forbidden status, production storage enforcement false, and no secret/env exposure;
- Registry Artifact Trust panel rendering, trust summaries, mock signing boundary status, no-real-signing/no-real-verification/no-external-registry-call metadata, resolver gate preservation, and no secret/env exposure;
- Eval Suite Execution Harness panel rendering, deterministic mock suite/run/verdict metadata, external eval disabled status, canary/apply blocked relationship, and no secret/env exposure;
- GitHub App controlled runtime panel rendering and no private-key/token/webhook-secret exposure;
- API provider mapping;
- explicit demo fallback;
- static renderer behavior.

Existing dashboard/security tests still verify demo rendering and redaction behavior.

## Known Limitations

- Dashboard read models expose Auth/RBAC v0 mock actor context, Tenant/Repo/Provider Scope Model v1 mock/readiness scope summaries, Dashboard/Readiness Tenant Scope Planning v1 inventories, Dashboard/Readiness Tenant Scope Implementation v1 metadata hints, Tenant Scope Enforcement v1 partial metadata, Audit Query Scope Enforcement v1 partial metadata, Registry Artifact Trust v1 metadata, and Eval Suite Execution Harness v1 metadata, but they are not production tenant/RBAC scoped.
- Auth/RBAC Production Readiness read models are planning-only; they do not implement real auth, sessions, IdP calls, tenant isolation, or service-account credential issuance.
- Empty API state can produce empty sections until workflows or API actions create records.
- Demo fallback still runs deterministic fixture workflows for static/offline tests.
- Phase 4 improvement/governance read models surface existing in-memory state only.
- Eval Suite Execution Harness read models surface in-memory mock eval metadata only; no real external eval, canary, or apply workflow is executed.
- Registry/governance dashboard/readiness views are not tenant/team scoped yet; RequestContext metadata is attribution-only in v1.
- No streaming UI or production analytics layer is implemented.
- Database operations read models are planning/readiness metadata only; they do not make persistent DB operations production-ready.
- Secret backend migration read models are planning/readiness metadata only; they do not implement production secret management.
- GitHub App controlled runtime read models are mock/status metadata only; they do not implement live installation discovery, private-key signing, or installation-token exchange.
- GitHub App integration-test read models are readiness metadata only; they do not run live tests, call GitHub, exchange installation tokens, or expose env values.
- Production secret backend decision read models are planning metadata only; they do not call Vault/cloud/custom backends, read/migrate/rotate secrets, issue credentials, or mark secrets production-ready.
- Vault secret backend read models are status metadata only; they do not call Vault in default runtime/tests, expose Vault tokens or secret values, migrate/rotate secrets, issue credentials, or mark secrets production-ready.
- Staging deployment read models are planning/readiness metadata only; they do not deploy staging or make staging production-ready.
- Staging deployment dry-run read models are readiness aggregation metadata only; they do not deploy, run CI jobs, execute remote integration tests, call providers, mutate resources, or make staging production-ready.
- Staging release candidate read models are checklist/readiness metadata only; they do not create releases, Git tags, GitHub releases, deploy, run CI jobs, execute remote integration tests, call providers, mutate resources, or make staging production-ready.
- Staging deployment execution read models are planning metadata only; they do not create releases, Git tags, deploy, run deployment commands, execute remote integration tests, call providers, mutate resources, or make staging production-ready.
- Staging CI/CD read models are planning/readiness metadata only; they do not create active workflows, run jobs, upload artifacts, run remote integrations by default, deploy staging, or make staging production-ready.
- Agent Workspace Lifecycle v2 read models are in-memory metadata only; they do not allocate real Git worktrees, inspect live Git status for cleanup, or delete user workspaces.
- Agent Worktree Allocation v1 read models are in-memory metadata only; they validate dry-run/fixture-only allocation requests and do not run real worktree commands, reserve distributed locks, or delete workspaces.
- Multi-session Agent Run Coordination v1 read models are in-memory metadata only; Merge Queue Policy v2 can display their readiness impact, but they do not persist distributed locks, allocate real branches/workspaces, run symbol-aware prediction, or execute agents.
- Multi-user / Multi-session Branch Orchestrator v2 read models are in-memory metadata only; Merge Queue Policy v2 can display branch ownership readiness impact, but they do not persist distributed branch locks, create real branches, inspect remote branch existence, allocate worktrees, or run real drift checks.
- Cross-session File Lease / Edit Intent Graph v1 read models are in-memory metadata only; Merge Queue Policy v2 can display edit overlap holds, but they do not persist distributed file leases, lock OS files, parse or mutate source files, or run symbol-aware prediction.
- Merge Queue Policy v2 read models are metadata-only. They show policy status, readiness decisions, holds, blocking reasons, warnings, priority order, and explicit disabled merge execution/auto-merge flags without executing Git or calling providers.
- Conflict Resolution Assistant v1 read models are metadata-only. They show summaries, review-only plans, recommendations, suggested validation, and explicit disabled apply/LLM/merge/source-mutation flags without inspecting source contents or applying changes.
- PR Ownership / Handoff Model v1 read models are metadata-only. They show owner/reviewer metadata, local handoff state, decisions, merge queue owner readiness, and explicit disabled remote PR update/reviewer assignment/GitHub API/auto-merge flags without mutating provider PRs or branches.
- Real Merge Execution Policy v1 read models are metadata-only. They show disabled merge/auto-merge/remote-push flags, latest decisions, preconditions, forbidden operations, and post-evidence template status without executing Git, updating PRs, mutating workspaces, or calling providers.

## Next Recommended Task

Tenant Scope Enforcement v1 and Audit Query Scope Enforcement v1 are implemented as partial representative metadata. Production Auth Provider Skeleton v1 is implemented as disabled/readiness-only provider metadata, Agent Workspace Lifecycle v2 is implemented as mock-first workspace metadata, Agent Worktree Allocation v1 is implemented as mock-first fixture-only allocation metadata, Multi-session Agent Run Coordination v1 is implemented as mock-first session coordination metadata, Multi-user / Multi-session Branch Orchestrator v2 is implemented as mock-first branch ownership metadata, Cross-session File Lease / Edit Intent Graph v1 is implemented as mock-first edit-intent metadata, Merge Queue Policy v2 is implemented as mock-first merge-readiness metadata, Conflict Resolution Assistant v1 is implemented as review-only conflict planning metadata, PR Ownership / Handoff Model v1 is implemented as local ownership metadata, Real Merge Execution Policy v1 is implemented as disabled merge execution policy/readiness metadata, Registry Artifact Trust v1 is implemented as mock-first signed-package trust metadata, Eval Suite Execution Harness v1 is implemented as deterministic mock eval metadata, and Canary Execution Harness + Apply Workflow v1 is implemented as deterministic mock canary plus metadata-only apply gating. Merge Queue Live Integration-Test Profile v1 is implemented as a skipped-by-default read-only readiness panel that consumes Merge Queue Policy v2 evidence and never enables real merge, remote rebase, force-push, branch deletion, or auto-merge. Next recommended task: External Observability Export v1 planning, Tenant Scope Enforcement v2, Eval/Canary Live Integration-Test Profile v1, Registry Signed Bundle Verification Planning v1, Real Merge Execution Approval Profile v1, or Merge Queue Live Dry-run Execution v1.
