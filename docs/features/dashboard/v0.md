# Dashboard API-backed Read Model Migration v0

## What Changed

Dashboard API-backed Read Model Migration v0 adds a read-only dashboard data layer:

- shared dashboard read-model DTO types in `packages/shared`;
- sanitized API read-model assembly in `apps/api/src/dashboard-read-model.ts`;
- `/dashboard/*` API endpoints for overview, tasks, Git, conflicts, registry, LLM, agents, policy, auth/RBAC, providers, security, Local Agent, MCP Gateway, production readiness, GitHub App hardening, observability, database operations, and audit sections;
- a web `DashboardDataProvider` abstraction with API and demo providers;
- dashboard rendering that consumes read models instead of directly constructing services;
- deterministic tests for API read models, provider mapping, fallback behavior, rendering, and no-secret exposure.

## Read Model Architecture

The API read-model assembler composes current service and repository state only. It does not create tasks, run workflows, evaluate new policy decisions, request secret leases, execute agent runs, create Local Agent fixture invocations, call GitHub, or call LLM providers.

The web layer consumes `DashboardReadModels` through:

- `ApiDashboardDataProvider`: fetches `/dashboard/*` endpoints.
- `DemoDashboardDataProvider`: builds deterministic fixture read models from the existing demo data path for static tests/offline rendering.

Runtime API mode can be enabled with:

```bash
AICHESTRA_DASHBOARD_DATA_SOURCE=api
AICHESTRA_DASHBOARD_API_BASE_URL=http://127.0.0.1:3000
```

If API mode is configured and the API is unavailable, demo fallback is used unless:

```bash
AICHESTRA_DASHBOARD_DISABLE_DEMO_FALLBACK=true
```

## API Endpoints

- `GET /dashboard/overview`
- `GET /dashboard/tasks`
- `GET /dashboard/git`
- `GET /dashboard/conflicts`
- `GET /dashboard/registry`
- `GET /dashboard/llm`
- `GET /dashboard/agents`
- `GET /dashboard/policy`
- `GET /dashboard/auth`
- `GET /dashboard/providers`
- `GET /dashboard/security`
- `GET /dashboard/local-agents`
- `GET /dashboard/mcp`
- `GET /dashboard/readiness`
- `GET /dashboard/github-app`
- `GET /dashboard/observability`
- `GET /dashboard/database`
- `GET /dashboard/audit`

Dashboard routes are read-only. Non-GET requests return `method_not_allowed`.

## Dashboard Sections

v0 exposes API-backed read models for:

- Task and TaskRun summaries;
- Real Git Adapter v2 gate/config/state/audit/webhook summaries;
- Conflict Manager leases, risks, queue, and simulations;
- Registry counts, entries, approval queue, packages, revisions, eval results, and improvement/governance summaries;
- LLM Gateway v2 config, remote gate state, models, routes, provider health, fallback policies, routing decisions, virtual keys, usage, audit, and blocked examples;
- Agent Runner config, runs, command results, workspaces, and audit;
- Policy rules, audit entries, and blocked examples;
- Production Auth/RBAC Planning v0 mock auth mode, current actor, roles, teams, service accounts, authorization examples, and auth audit events;
- Enterprise provider catalog, local CLI templates, local-agent descriptors, readiness, and audit;
- Secrets/Sandbox metadata, SecretRef-backed GitHub token/webhook and LLM credential status, network/redaction policies, credential audit, and security audit;
- Local Agent Protocol v1 registration, channels, handshakes, compatibility, consent, invocations, streams, and audit;
- MCP Gateway v0 mock server/tool catalog, risk summary, recent invocations, blocked examples, integration safety status, and MCP audit events;
- Production Deployment Readiness Planning v0 deployment profile, blocker, risk, and warning summaries;
- GitHub App / Production Webhook Hardening Planning v0 permission matrix, webhook allowlist, replay/dead-letter, credential, endpoint, risk, and no-secret summaries;
- Observability / Audit Retention v0 normalized audit, retention/redaction, metric, trace, source coverage, and blocker summaries;
- Persistent DB Production Operations v1 deployment profile, migration readiness, index review, retention/audit growth, webhook persistence, backup/restore, pooling, risk, and no-DB-url summaries;
- Secret Backend Migration Planning v0 backend option, migration phase, readiness check, risk, rotation plan, lease policy, env fallback warning, credential kind status, blocker, and no-secret/no-env summaries;
- aggregated audit groups.

## Safety Rules

- No GitHub calls.
- No LLM provider calls.
- No vendor CLI execution.
- No runner command execution.
- No workflow execution.
- No secret lease issuance.
- No MCP real transport or tool invocation from dashboard read routes.
- No credential cache reads.
- No production DB connection, migration execution, backup/restore job, destructive retention job, or database URL exposure.
- No real secret backend connection, secret migration, rotation job, credential issuance, or env value exposure.
- No raw unredacted logs.
- No tokens, provider API keys, or credential values in API/web output.

`sanitizeDashboardValue` masks token-like strings, sensitive env assignments, and credential-cache paths before read models are returned or rendered.

## Fallback Strategy

Demo fallback remains for deterministic static tests and offline local preview. It is explicit in `DemoDashboardDataProvider` and should not be treated as production dashboard state.

API-backed mode is available through `ApiDashboardDataProvider`; it fetches all `/dashboard/*` sections and maps them to the shared read-model DTOs.

## Tests

`tests/dashboard-read-model-v0.test.ts` covers:

- `/dashboard/*` endpoint availability;
- safety flags and blocked examples;
- no-secret exposure;
- auth/RBAC visibility and mock-auth warning;
- read-only behavior for dashboard routes;
- no Git audit mutation from read-model reads;
- database operations panel rendering and no database URL exposure;
- secret backend migration panel rendering and no env/secret value exposure;
- API provider mapping;
- explicit demo fallback;
- static renderer behavior.

Existing dashboard/security tests still verify demo rendering and redaction behavior.

## Known Limitations

- Dashboard read models expose Auth/RBAC v0 mock actor context, but they are not production tenant/RBAC scoped.
- Empty API state can produce empty sections until workflows or API actions create records.
- Demo fallback still runs deterministic fixture workflows for static/offline tests.
- Phase 4 improvement/governance read models surface existing in-memory state only.
- No streaming UI or production analytics layer is implemented.
- Database operations read models are planning/readiness metadata only; they do not make persistent DB operations production-ready.
- Secret backend migration read models are planning/readiness metadata only; they do not implement production secret management.

## Next Recommended Task

Secret Backend Migration Planning v0 is implemented as a read-only dashboard/readiness foundation. Next recommended task: Production Auth/RBAC v1 planning, or GitHub App controlled implementation v1 if secret backend planning is sufficiently mature.
