# Repository Inventory

## Summary

Real Integration Foundation v0 keeps the default runtime in-memory, and Persistent DB v1 adds opt-in Postgres repositories for the core durable slice. Phase 4 improvement/governance stores remain in-memory in v1. Persistence priorities:

- `persistent_required`: must be durable before real provider writes or production usage.
- `persistent_recommended`: should become durable soon, but can be recomputed or mocked during integration planning.
- `ephemeral_ok`: can be derived or request-scoped.
- `test_only`: should remain test fixture infrastructure.

## Dashboard Read Models

Dashboard API-backed Read Model v0 does not add persistent storage. `/dashboard/*` routes derive read-only DTOs from existing repositories, service config, and audit repositories, then sanitize output through shared read-model helpers. `DemoDashboardDataProvider` remains a test/offline fixture path and is not production persistence.

Production Deployment Readiness Planning v0, GitHub App / Production Webhook Hardening Planning v0, Persistent DB Production Operations v1, Secret Backend Migration Planning v0, Production Secret Backend Implementation Option Decision v0, Production Auth/RBAC v1 Planning, Policy Bundle / OPA-Cedar Planning v0, Staging Deployment Profile v0, Staging Deployment Dry-run Profile v0, Staging Release Candidate Checklist v0, Staging Deployment Execution Plan v0, Staging CI/CD Pipeline Planning v0, GitHub App integration-test profile v1, LLM Gateway integration-test profile v1, and Vault Integration-Test Profile v1 add read-only seeded deployment, GitHub App, database operations, secret backend migration, secret backend decision, production auth, policy bundle, staging, staging dry-run, staging RC, staging execution, CI/CD, GitHub App integration-test, LLM integration-test, and Vault integration-test readiness models in `packages/deployment-readiness`. These are planning/readiness metadata only and do not require persistence in their current milestones. GitHub App Controlled Implementation v1 adds config-derived runtime/read models and mock token-handle audit metadata; those also remain ephemeral/config-derived in v1.

| Repository or Store | Current Implementation | Priority | Recommended Table | Key Indexes | Retention | Audit Requirements |
|---|---|---:|---|---|---|---|
| DashboardReadModels | Derived in `apps/api/src/dashboard-read-model.ts`; consumed by `ApiDashboardDataProvider` | ephemeral_ok | future materialized dashboard views only if needed | section, generated_at, tenant/project when auth exists | Recompute from source tables | Must not trigger workflows/provider calls or expose secrets/tokens/unredacted logs. |
| Dashboard demo fallback | `DemoDashboardDataProvider` over deterministic `apps/web/lib/mock-data.ts` fixtures | test_only | none | none | Test/offline render lifetime | Must remain explicit and must not be used as production state. |
| DeploymentReadiness seed model | `packages/deployment-readiness` read-only seed data surfaced by `/readiness/deployment/*` and `/dashboard/readiness` | ephemeral_ok | future `deployment_profiles`, `deployment_readiness_checks`, `production_risks` only if runtime administration is needed | profile_id, status, severity | Planning docs lifecycle | Must not run live infra checks, store secrets, or mark production ready. |
| DatabaseOperationsReadiness seed model | `packages/deployment-readiness` read-only DB operations data surfaced by `/readiness/database/*`, `/dashboard/database`, and `/health` metadata | ephemeral_ok | future `database_readiness_checks`, `database_operation_risks`, `database_migration_status` only if runtime administration is needed | profile_id, category, status, severity, migration_id | Planning docs lifecycle | Must not connect to production DBs, expose DB URL values, execute migrations, run backups/restores, delete data, or mark production ready. |
| SecretBackendMigrationReadiness seed model | `packages/deployment-readiness` read-only secret backend migration data surfaced by `/readiness/secrets/*`, `/dashboard/secret-backend`, and `/health` metadata | ephemeral_ok | future `secret_backend_options`, `secret_backend_migration_phases`, `secret_backend_readiness_checks`, `secret_backend_risks`, `secret_rotation_plans`, `secret_lease_policies` only if runtime administration is needed | backend_kind, category, status, severity, secret_kind | Planning docs lifecycle | Must not connect to real secret backends, expose secret/env values, rotate secrets, issue credentials, read caches, or mark production secret management ready. |
| SecretBackendOptionDecision seed model | `packages/deployment-readiness` read-only secret backend decision data surfaced by `/readiness/secret-backend-decision/*`, `/dashboard/secret-backend-decision`, and `/health` metadata | ephemeral_ok | future `secret_backend_decisions`, `secret_backend_decision_scores`, `secret_backend_provider_mappings`, `secret_backend_decision_risks` only if runtime administration is needed | backend_kind, criterion_id, severity, provider_value | Planning docs lifecycle | Must not call Vault/cloud/custom backends from the decision surface, expose secret/env values, migrate or rotate secrets, issue credentials, read caches, or mark production secrets ready. |
| VaultSecretBackend runtime metadata | `packages/security` Vault v1 provider config, health, readiness checks, sanitized audit, and credential lease/handle metadata surfaced by `/readiness/secrets/vault/*`, `/security/secrets/vault/*`, `/dashboard/vault-secret-backend`, and `/health` | ephemeral_ok for v1 config/readiness; credential lease/audit follows existing security repository lifecycle | future `secret_backend_configs`, `secret_backend_health`, `credential_resolution_audit` only if runtime administration becomes durable | provider_kind, secret_ref_id, secret_kind, status, event_type | Security service lifecycle | Must not expose Vault tokens, Vault address values, secret values, env values, broad Vault paths, or credential cache paths. Default runtime/tests must not call Vault. |
| VaultIntegrationTestReadiness seed model | `packages/deployment-readiness` read-only Vault integration-test profile data surfaced by `/readiness/vault-integration/*`, `/dashboard/vault-integration`, and `/health` metadata | ephemeral_ok | future `vault_integration_test_profiles`, `vault_integration_test_runs` only if controlled live Vault testing becomes operationalized | category, status, severity, backend_kind | Planning docs lifecycle | Must not call Vault by default, expose Vault token/address/path/key/secret/env values, write/delete/rotate secrets, broadly list paths, read credential caches, or mark Vault production-ready. |
| AuthRbacProductionReadiness seed model | `packages/deployment-readiness` read-only production auth data surfaced by `/readiness/auth/*`, `/dashboard/auth-production`, and `/health` metadata | ephemeral_ok | future `auth_provider_options`, `auth_rbac_migration_phases`, `auth_rbac_readiness_checks`, `auth_rbac_production_risks`, `tenant_boundary_plans`, `service_account_plans`, `production_rbac_permission_matrix` only if runtime administration is needed | provider_kind, category, status, severity, tenant_kind, service_account_kind, role_name | Planning docs lifecycle | Must not call IdPs, issue sessions/JWTs, expose tokens/cookies/session ids/raw assertions/passwords, issue service-account credentials, or mark production auth ready. |
| StagingDeploymentReadiness seed model | `packages/deployment-readiness` read-only staging profile data surfaced by `/readiness/staging/*`, `/dashboard/staging`, and `/health` metadata | ephemeral_ok | future `staging_profiles`, `staging_integration_gates`, `staging_readiness_checks`, `staging_promotion_criteria`, `staging_rollback_criteria` only if runtime administration is needed | integration_kind, category, status, severity | Planning docs lifecycle | Must not deploy resources, call providers, expose secrets/env values, enable production traffic, or mark staging production-ready. |
| StagingDeploymentExecutionReadiness seed model | `packages/deployment-readiness` read-only staging execution data surfaced by `/readiness/staging-execution/*`, `/dashboard/staging-execution`, and `/health` metadata | ephemeral_ok | future `staging_execution_plans`, `staging_execution_gates`, `staging_go_no_go_decisions`, `staging_rollback_plans` only if execution administration becomes durable | phase, category, status, severity, owner_role | Planning docs lifecycle | Must not deploy, create releases or tags, run deployment commands, call providers, run remote integration tests, issue credentials, expose secrets/env values, or mark staging deployed/production ready. |
| LLMIntegrationTestReadiness seed model | `packages/deployment-readiness` read-only LLM integration-test profile data surfaced by `/readiness/llm-integration/*`, `/dashboard/llm-integration`, and `/health` metadata | ephemeral_ok | future `llm_integration_test_profiles`, `llm_integration_test_runs` only if controlled live integration testing becomes operationalized | category, status, severity, provider_kind | Planning docs lifecycle | Must not call LLM providers by default, expose API keys/env/base URL values/raw prompts/raw provider responses, enable streaming/tool calls, execute vendor CLI, read credential caches, or mark LLM production-ready. |

## Auth/RBAC

Production Auth/RBAC Planning v0 uses in-memory repositories in `@aichestra/auth`. Persistent auth storage is not required for v0, but these tables are recommended before production SSO/SCIM/session/service-account work.

| Repository or Store | Current Implementation | Priority | Recommended Table | Key Indexes | Retention | Audit Requirements |
|---|---|---:|---|---|---|---|
| PrincipalRepository | `InMemoryAuthRepository` | persistent_required before production auth | `auth_principals` | `id`, `principal_kind`, `identity_provider_id`, `external_subject`, `status` | Identity lifecycle | No password, token, session secret, or provider credential material. Status changes must be audited later. |
| ActorRepository | `InMemoryAuthRepository` | persistent_required before production auth | `auth_actors` | `id`, `principal_id`, `actor_kind`, `status` | Actor lifecycle | Request-time actor metadata only; no secrets. |
| TeamRepository | `InMemoryAuthRepository` | persistent_required before SCIM/team sync | `auth_teams` | `id`, `name`, `status` | Org/team lifecycle | Team changes must be auditable once mutable. |
| RoleRepository | `InMemoryAuthRepository` | persistent_required before production RBAC | `auth_roles` | `id`, `name`, `status` | Role lifecycle | Permission bundles only; no secrets. |
| PermissionRepository | `InMemoryAuthRepository` | persistent_required before production RBAC | `auth_permissions` | `id`, `action`, `resource_kind`, `risk_level` | Permission catalog lifecycle | Static/catalog changes should be reviewed and audited. |
| RoleBindingRepository | `InMemoryAuthRepository` | persistent_required before production RBAC | `auth_role_bindings` | `id`, `principal_id`, `team_id`, `role_id`, `scope_kind`, `scope_id`, `status` | Binding lifecycle | Binding evaluation is audited; mutations need production auth audit later. |
| ServiceAccountRepository | `InMemoryAuthRepository` | persistent_required before production service accounts | `auth_service_accounts` | `id`, `principal_id`, `owner_team_id`, `status` | Service account lifecycle | Store metadata/scopes only. Never store raw service account secrets or API keys. |
| IdentityProviderRepository | `InMemoryAuthRepository` | persistent_required before SSO/SCIM | `auth_identity_providers` | `id`, `provider_kind`, `status` | Provider config lifecycle | Store provider metadata only; no client secrets or IdP tokens. |
| AuthAuditRepository | `InMemoryAuthRepository` | persistent_required before production auth | `auth_audit_events` | `id`, `event_type`, `actor_id`, `principal_id`, `action`, `resource_kind`, `created_at` | Long-lived compliance retention | Append-only; must not include tokens, cookies, passwords, API keys, session secrets, provider credentials, or credential-cache contents. |

## Phase 1

| Repository or Store | Current Implementation | Priority | Recommended Table | Key Indexes | Retention | Audit Requirements |
|---|---|---:|---|---|---|---|
| TaskRepository | `InMemoryAichestraStore`; opt-in `PostgresAichestraStore` | persistent_required | `tasks` | `id`, `repo_id`, `status`, `requester_user_id`, `created_at` | Project lifetime | Status changes require audit events. |
| TaskRunRepository | `InMemoryAichestraStore`; opt-in `PostgresAichestraStore` | persistent_required | `task_runs` | `id`, `task_id`, `(task_id, attempt)`, `status` | Project lifetime | Run creation, finish, failure, and retry should be audited. |
| UsageLedgerRepository | `InMemoryAichestraStore.recordUsage`; opt-in `PostgresAichestraStore.recordUsage` | persistent_required | `usage_ledger_entries` | `id`, `task_id`, `task_run_id`, `user_id`, `provider`, `created_at` | Long-lived billing/audit retention | Must preserve taskId/taskRunId attribution. |
| RepoRepository | `InMemoryAichestraStore`; opt-in `PostgresAichestraStore`; used by Real Git Adapter v1 | persistent_required before real Git writes | `repos` | `id`, `(provider, owner, name)` | Project lifetime | Repo creation/config changes and remote config validation should be audited. |
| PullRequestRepository | `InMemoryAichestraStore`; opt-in `PostgresAichestraStore`; used by Real Git Adapter v1 | persistent_required before real Git writes | `pull_requests` | `id`, `task_id`, `repo_id`, `provider`, `external_id`, `status` | Until task/PR archival plus audit retention | PR creation and provider metadata updates require sanitized audit. GitHub PR numbers are stored as provider external ids. |
| GitAuditRepository | Generic `AuditLog` via `GitIntegrationService.listGitAuditEvents` | persistent_required before real Git writes | `audit_events` or future `git_audit_events` | `id`, `action`, `repo_id`, `task_id`, `created_at` | Long-lived operational/audit retention | Must not store tokens, secrets, raw credentials, or raw unsafe command output. v1 remote branch/PR/changed-file events use sanitized metadata. |
| GitWebhookEventRepository | `InMemoryAichestraStore`; opt-in `PostgresAichestraStore`; used by Real Git Adapter v2 | persistent_required before webhook ingestion | `git_webhook_events` | `id`, `delivery_id`, `repo_ref`, `event_type`, `status`, `received_at` | Webhook audit/debug retention | Stores delivery/event metadata, payload hash, and verification status only. Raw payloads, webhook secrets, tokens, and credentials must not be stored. |
| GitWebhookVerificationRepository | `InMemoryAichestraStore`; opt-in `PostgresAichestraStore`; used by Real Git Adapter v2 | persistent_required before webhook ingestion | `git_webhook_verification_results` | `delivery_id`, `created_at` | Webhook audit/debug retention | Stores algorithm/result/reason only; never stores webhook secrets or raw signatures beyond sanitized status metadata. |
| GitPullRequestSyncStateRepository | `InMemoryAichestraStore`; opt-in `PostgresAichestraStore`; used by Real Git Adapter v2 | persistent_required before webhook ingestion | `git_pull_request_sync_states` | `(repo_ref, pull_request_number)`, `repo_id`, `task_id`, `task_run_id`, `state` | Until PR/task archival plus audit retention | Durable read model for mapped or external PRs. Sanitized metadata only; no raw provider payloads or tokens. |
| GitBranchSyncStateRepository | `InMemoryAichestraStore`; opt-in `PostgresAichestraStore`; used by Real Git Adapter v2 | persistent_required before webhook ingestion | `git_branch_sync_states` | `(repo_ref, branch_name)`, `repo_id`, `exists` | Until branch/task archival plus audit retention | Durable branch metadata read model. Deleted remote refs may set `exists=false`; no branch deletion operation is performed. |
| GitWebhookAuditRepository | `InMemoryAichestraStore`; opt-in `PostgresAichestraStore`; used by Real Git Adapter v2 | persistent_required before webhook ingestion | `git_webhook_audit_events` | `event_type`, `delivery_id`, `repo_ref`, `created_at` | Long-lived operational/audit retention | Append-only sanitized webhook receive/verify/process/sync audit. Must not contain webhook secrets, GitHub tokens, raw credentials, or raw payloads. |
| BranchOrchestratorRepository | `InMemoryBranchOrchestratorRepository` in `packages/git-adapter` | persistent_required before production branch orchestration | `branch_orchestration_requests`, `branch_orchestration_decisions`, `branch_ownership_records`, `base_branch_drift_statuses`, `branch_naming_policies` | `repo_id`, `base_branch`, `branch_name`, `user_id`, `task_id`, `task_run_id`, `agent_run_id`, `session_id`, `branch_lease_id`, `workspace_lease_id`, `status`, `decision` | Retain with AgentRun, BranchLease, and merge queue history | Stores branch allocation and ownership metadata only. Must not create branches, mutate workspaces, run Git, call providers, expose secrets/env values, or weaken existing safety gates. |
| BranchOrchestratorAuditRepository | `InMemoryBranchOrchestratorRepository` audit store | persistent_required before production branch orchestration | `branch_orchestrator_audit_events` | `id`, `repo_id`, `branch_name`, `request_id`, `decision_id`, `ownership_record_id`, `event_type`, `correlation_id`, `created_at` | Long-lived operational/audit retention | Append-only sanitized branch orchestration events. Never store tokens, credential cache paths, raw env values, workspace contents, or full sensitive local paths. |
| GitHubAppRuntimeConfig read model | Config-derived in `packages/adapters/src/git/github-app.ts`; surfaced through `GitHubAppRuntimeService` | ephemeral_ok in v1 | future `github_app_runtime_configs` only if live GitHub App administration is implemented | app id, status, auth mode | Config lifecycle | Metadata only. Must not store private keys, JWTs, installation tokens, webhook secrets, legacy PATs, or raw credential values. |
| GitHubAppInstallationState read model | Config-derived from allowlisted installation ids | ephemeral_ok in v1 | future `github_app_installations` before live installation sync | installation id, account login, status | Installation lifecycle future | Metadata only. Must not imply live installation discovery or permission grant. |
| GitHubAppRepositoryGrantState read model | Config-derived from allowlisted repo refs | ephemeral_ok in v1 | future `github_app_repository_grants` before production app auth | installation id, repo owner/name, status | Repo grant lifecycle future | Metadata only. No workflow/admin/secrets/deployment permissions by default. |
| GitHubInstallationTokenResult audit/read model | In-memory sanitized audit in `GitHubAppRuntimeService`; token handles are transient metadata | persistent_recommended before live app auth | future `github_app_token_audit_events` or common audit table | request id, installation id, repo ref, status, created_at | Short operational retention plus security audit | Must store status and token handle id only. Never store raw installation tokens or private-key material. |

## LLM Gateway

| Repository or Store | Current Implementation | Priority | Recommended Table | Key Indexes | Retention | Audit Requirements |
|---|---|---|---|---|---|---|
| ModelCatalogRepository | `InMemoryModelCatalogRepository` | persistent_recommended before real LLM providers | `llm_models` | `id`, `provider_kind`, `status` | Project/model lifecycle | Model create/status changes should be audited. |
| VirtualModelKeyRepository | `InMemoryVirtualModelKeyRepository` | persistent_required before real multi-user usage | `virtual_model_keys` | `id`, `owner_kind`, `owner_id`, `status` | Key policy lifecycle | Must not store provider API secrets. Status changes should be audited. |
| LLMAuditRepository | `InMemoryLLMAuditRepository` | persistent_required for LLM Gateway v1 remote calls before production usage | `llm_audit_events` | `id`, `task_id`, `task_run_id`, `provider_kind`, `model_id`, `created_at` | Long-lived or compliance retention | Append-only; never log API keys, bearer tokens, raw prompts, unredacted output, or credential cache contents. |
| LLMGatewayRequestRepository | not implemented; request ids live in result metadata | persistent_recommended before production real provider calls | `llm_gateway_requests` | `id`, `task_id`, `task_run_id`, `provider_kind`, `model_id`, `created_at` | Debug/audit retention | Store sanitized request/result metadata only. LLM Gateway v1 does not persist raw requests. |
| LLMRouteRepository | `InMemoryLLMRouteRepository` in `packages/llm-gateway/src/routing.ts` | persistent_recommended before production route administration | `llm_routes` | `id`, `provider_id`, `provider_kind`, `model_id`, `enabled`, `priority` | Route config lifecycle | Routes contain provider/model metadata only. Must not store API keys, bearer tokens, raw prompts, provider credential values, or credential cache paths. |
| LLMFallbackPolicyRepository | `InMemoryLLMFallbackPolicyRepository` | persistent_recommended before production fallback governance | `llm_fallback_policies` | `id`, `enabled`, `max_attempts` | Policy lifecycle | Fallback must remain bounded and audited. Policy/auth/budget/credential denials must not be bypassed. |
| LLMRoutingDecisionRepository | `InMemoryLLMRoutingDecisionRepository` | persistent_required before real multi-provider production usage | `llm_routing_decisions` | `id`, `request_id`, `selected_provider_id`, `selected_model_id`, `decision`, `created_at` | Operational/audit retention | Store selected route ids, decision ids, and sanitized reasons only. No secrets, raw prompts, or unredacted output. |
| LLMProviderHealth read model | derived from provider config/catalog/routes | ephemeral_ok in v2 | `llm_provider_health_snapshots` future table if needed | `provider_id`, `provider_kind`, `status`, `last_checked_at` | Recompute or short-lived snapshots | Health metadata must expose booleans/status only, never secret values. |
| ProviderCatalogRepository | `InMemoryProviderCatalogRepository` | persistent_recommended before real enterprise provider management | `provider_catalog_entries` | `id`, `vendor`, `kind`, `status` | Provider lifecycle | Auth config must contain references only, never raw tokens or credential cache paths. |
| ProviderAuditRepository | `InMemoryProviderAuditRepository` | persistent_required before real provider calls or Local Agent invocation | `provider_audit_events` | `id`, `provider_id`, `event_type`, `task_id`, `task_run_id`, `created_at` | Compliance/audit retention | Append-only; sanitized metadata only; no raw prompts, tokens, or credential cache contents. |
| LocalAgentRegistrationRepository | `InMemoryLocalAgentRegistrationRepository` in `packages/llm-gateway` | persistent_required before real Local Agent pairing | `local_agent_registrations` | `id`, `user_id`, `host_id`, `status`, `last_seen_at` | Agent connection lifecycle | Must not store vendor credentials or local credential cache content. |
| LocalAgentSessionRepository | `InMemoryLocalAgentSessionRepository` | persistent_required before real Local Agent sessions | `local_agent_sessions` | `id`, `agent_id`, `user_id`, `status`, `expires_at` | Session lifecycle plus audit retention | Session metadata only; no bearer tokens or provider credentials. |
| LocalAgentChannelRepository | `InMemoryLocalAgentChannelRepository` | persistent_required before real Local Agent pairing | `local_agent_channels` | `id`, `agent_id`, `user_id`, `status`, `channel_kind`, `expires_at` | Channel lifecycle and consent evidence | Mock channel metadata only; no real transport keys or credential material. |
| LocalAgentHandshakeRepository | `InMemoryLocalAgentHandshakeRepository` | persistent_required before real Local Agent pairing | `local_agent_handshakes` | `id`, `channel_id`, `agent_id`, `response_status`, `expires_at` | Pairing lifecycle evidence | Mock challenges only; no production private keys or credential cache content. |
| LocalAgentCapabilityAdvertisementRepository | `InMemoryLocalAgentCapabilityAdvertisementRepository` | persistent_required before real Local Agent compatibility enforcement | `local_agent_capability_advertisements` | `id`, `agent_id`, `agent_version`, `advertised_at` | Agent capability history | Capabilities are not permissions; no secrets. |
| LocalCliCompatibilityRepository | `InMemoryLocalCliCompatibilityRepository` | persistent_recommended before real Local Agent compatibility enforcement | `local_cli_compatibility_entries`, `local_cli_compatibility_results` | `provider_template_id`, `agent_id`, `provider_id`, `created_at` | Compatibility audit/debug retention | Fixture versions and parser metadata only; no real CLI execution output. |
| LocalAgentInvocationEnvelopeRepository | `InMemoryLocalAgentInvocationEnvelopeRepository` | persistent_required before real Local Agent dispatch | `local_agent_invocation_envelopes` | `id`, `task_id`, `task_run_id`, `provider_id`, `local_agent_id`, `created_at` | Retain with invocation/audit history | Envelope stores refs, hashes, policy ids, and mock signature status only; no raw prompts or secrets. |
| LocalAgentInvocationRepository | `InMemoryLocalAgentInvocationRepository` | persistent_required before real Local Agent dispatch | `local_agent_invocations` | `id`, `envelope_id`, `state`, `created_at`, `completed_at` | Retain with task/run history | Lifecycle transitions and redaction status must be auditable. |
| LocalAgentConsentRequestRepository | `InMemoryLocalAgentConsentRequestRepository` | persistent_required before real Local Agent dispatch | `local_agent_consent_requests` | `id`, `invocation_id`, `user_id`, `consent_level`, `expires_at` | Consent evidence retention | Consent request reasons must be sanitized. |
| LocalAgentConsentDecisionRepository | `InMemoryLocalAgentConsentDecisionRepository` | persistent_required before real Local Agent dispatch | `local_agent_consent_decisions` | `id`, `consent_request_id`, `user_id`, `decision`, `decided_at` | Consent evidence retention | Append-only consent decisions; no raw secrets. |
| LocalAgentInvocationStreamRepository | `InMemoryLocalAgentInvocationStreamRepository` | persistent_required before real Local Agent output retention | `local_agent_invocation_streams` | `id`, `invocation_id`, `state`, `started_at` | Bounded event retention | Stream state only; no raw unredacted output. |
| LocalAgentStreamEventRepository | `InMemoryLocalAgentStreamEventRepository` | persistent_required before real Local Agent output retention | `local_agent_stream_events` | `stream_id`, `invocation_id`, `sequence`, `source` | Bounded event retention | Store size-limited redacted previews only. |
| LocalAgentNormalizedEventRepository | `InMemoryLocalAgentNormalizedEventRepository` | persistent_required before real Local Agent output retention | `local_agent_normalized_events` | `id`, `invocation_id`, `source`, `type`, `created_at` | Bounded event retention | Store size-limited redacted stdout/stderr/system payloads only. |
| LocalAgentProtocolAuditRepository | `InMemoryLocalAgentProtocolAuditRepository` | persistent_required before real Local Agent dispatch | `local_agent_protocol_audit_events` | `id`, `event_type`, `agent_id`, `invocation_id`, `task_id`, `task_run_id`, `created_at` | Long-lived compliance retention | Append-only; no provider API keys, bearer tokens, OAuth tokens, or credential cache contents. |
| PolicyDecisionAuditRepository | `InMemoryPolicyDecisionAuditRepository` in `packages/policy`; schema skeleton only for Postgres | persistent_required before real provider integrations | `policy_decision_audit_entries` | `policy_decision_id`, `action`, `resource_kind`, `actor_id`, `task_id`, `task_run_id`, `created_at` | Long-lived for compliance | Append-only; must not store secrets or raw prompts. |

## MCP Gateway

MCP Gateway v0 uses in-memory repositories in `packages/mcp-gateway`. Future persistence is recommended before real MCP transport or production tool governance.

| Repository or Store | Current Implementation | Priority | Recommended Table | Key Indexes | Retention | Audit Requirements |
|---|---|---|---|---|---|---|
| MCPServerCatalogRepository | `InMemoryMCPServerCatalogRepository` | persistent_recommended before real MCP transport | `mcp_servers` | `id`, `server_kind`, `status`, `owner_team_id` | Server catalog lifecycle | Server metadata only; no connection secrets, tokens, or credential cache paths. |
| MCPToolDefinitionRepository | `InMemoryMCPToolDefinitionRepository` | persistent_recommended before real MCP tool governance | `mcp_tools` | `id`, `server_id`, `name`, `risk_level`, `status` | Tool catalog lifecycle | Tool schemas and refs only; `required_secret_refs` store ids only, never values. |
| MCPToolInvocationRepository | `InMemoryMCPToolInvocationRepository` | persistent_required before real MCP invocation | `mcp_invocations` | `id`, `request_id`, `server_id`, `tool_id`, `status`, `created_at` | Operational/audit retention | Store status, redacted previews, policy/auth decision ids, and sanitized metadata only. No raw output or secrets. |
| MCPToolAuditRepository | `InMemoryMCPToolAuditRepository` | persistent_required before real MCP invocation | `mcp_audit_events` | `id`, `event_type`, `server_id`, `tool_id`, `actor_id`, `created_at` | Long-lived compliance retention | Append-only sanitized metadata. Must not store raw secrets, API keys, bearer tokens, credential cache content, or unredacted tool output. |

## Policy-as-code

| Repository or Store | Current Implementation | Priority | Recommended Table | Key Indexes | Retention | Audit Requirements |
|---|---|---|---|---|---|---|
| PolicyRule store | Static code-defined rules in `createDefaultPolicyRules` | persistent_recommended before production policy management | `policy_rules` future table or versioned policy bundle store | `id`, `action`, `resource_kind`, `enabled`, `priority` | Policy bundle lifecycle | Rule changes must be reviewed and audited. Not implemented in v0. |
| Policy bundle readiness store | Deterministic seed data in `packages/deployment-readiness` | persistence_not_required_for_v0 | future `policy_bundles`, `policy_bundle_reviews`, `policy_bundle_test_results`, `policy_bundle_rollouts` | bundle id, version, domain, review status, signature metadata, rollout state | Policy Bundle / OPA-Cedar Planning v0 | Read-only planning only; no bundle parser, dynamic execution, external policy engine, signing, rollout, or rollback exists. |
| PolicyDecisionAuditRepository | `InMemoryPolicyDecisionAuditRepository` | persistent_required before real integrations | `policy_decision_audit_entries` | `policy_decision_id`, `action`, `resource_kind`, `actor_id`, `task_id`, `task_run_id`, `created_at` | Compliance/audit retention | Store decision summaries only; no secrets, tokens, API keys, or raw prompts. |

## Local Agent Runner

| Repository or Store | Current Implementation | Priority | Recommended Table | Key Indexes | Retention | Audit Requirements |
|---|---|---|---|---|---|---|
| AgentRunRepository | `InMemoryAgentRunRepository` in `packages/runner` | persistent_required before controlled local execution | `agent_runs` | `id`, `task_id`, `task_run_id`, `runner_kind`, `status`, `created_at` | Retain with TaskRun history | Runner start, completion, failure, and block status should be auditable. |
| AgentRunAuditRepository | `InMemoryAgentRunAuditRepository` in `packages/runner` | persistent_required before controlled local execution | `agent_run_audit_events` | `id`, `task_id`, `task_run_id`, `runner_kind`, `event_type`, `created_at` | Long-lived operational/audit retention | Append-only; never store secrets or unsafe command output. |
| InstructionAssemblyRepository | `InMemoryInstructionAssemblyRepository` in `packages/runner` | persistent_recommended before real runner execution | `instruction_assemblies` | `id`, `task_id`, `task_run_id`, `instruction_set_hash` | Retain with TaskRun history | Must preserve selected refs and hash used for runner input. |
| AgentWorkspaceRepository | `InMemoryAgentWorkspaceRepository` in `packages/runner` | persistent_recommended before wider local runner use | `agent_workspaces` | `id`, `task_id`, `task_run_id`, `status`, `created_at` | Retain with AgentRun history | Workspace status and cleanup must be auditable; never store secrets or workspace contents. |
| AgentWorkspaceLifecycleRepository | `InMemoryAgentWorkspaceLifecycleRepository` in `packages/runner` | persistent_recommended before concurrent real runner/worktree use | `agent_workspace_leases` | `id`, `task_id`, `task_run_id`, `agent_run_id`, `repo_id`, `branch_lease_id`, `status`, `workspace_kind` | Retain with AgentRun and BranchLease history | Stores per-AgentRun workspace lease metadata only. Must not store workspace contents, raw secrets, env values, or unredacted local paths in API/dashboard DTOs. |
| AgentWorkspaceLifecycleEventRepository | `InMemoryAgentWorkspaceLifecycleRepository` event store in `packages/runner` | persistent_recommended before concurrent real runner/worktree use | `agent_workspace_lifecycle_events` | `workspace_lease_id`, `event_type`, `timestamp`, `request_id`, `correlation_id` | Retain with AgentRun audit history | Append-only lifecycle event metadata. Must not imply real Git worktree execution or destructive cleanup. |
| AgentWorkspaceCleanupDecisionRepository | `InMemoryAgentWorkspaceLifecycleRepository` cleanup decision store in `packages/runner` | persistent_recommended before production cleanup governance | `agent_workspace_cleanup_decisions` | `workspace_lease_id`, `decision`, `evaluated_at`, `merge_status` | Retain with AgentRun and merge-readiness history | Decision-only in v2. Must not delete user workspaces, expose full local paths, or store secrets/env values. |
| AgentRunCoordinationRepository | `InMemoryAgentRunCoordinationRepository` in `packages/runner` | persistent_recommended before production multi-session concurrency control | `agent_sessions`, `agent_run_coordination_groups`, `agent_session_overlaps`, `agent_concurrency_policies` | `repo_id`, `base_branch`, `task_id`, `user_id`, `agent_run_id`, `branch_lease_id`, `workspace_lease_id`, `status`, `overlap_kind`, `severity` | Retain with AgentRun, BranchLease, and workspace lease history | Stores coordination metadata only. Must not execute agents, mutate workspaces, create branches, call providers, expose secrets/env values, or weaken policy gates. |
| AgentRunCoordinationAuditRepository | `InMemoryAgentRunCoordinationRepository` audit store in `packages/runner` | persistent_required before production concurrency control | `agent_run_coordination_audit_events` | `id`, `repo_id`, `session_id`, `group_id`, `event_type`, `request_id`, `correlation_id`, `created_at` | Long-lived operational/audit retention | Append-only sanitized coordination events. Never store tokens, credential cache paths, raw env values, or workspace contents. |
| EditIntentGraphRepository | `InMemoryEditIntentGraphRepository` in `packages/runner` | persistent_recommended before production multi-session edit coordination | `file_leases`, `edit_intents`, `edit_intent_graph_nodes`, `edit_intent_graph_edges`, `edit_overlap_assessments` | `repo_id`, `session_id`, `owner_session_id`, `agent_run_id`, `task_id`, `branch_name`, `workspace_lease_id`, `file_path_hash`, `status`, `overlap_kind`, `severity`, `recommendation` | Retain with AgentRun, BranchLease, workspace lease, and merge-readiness history | Stores file lease and edit-intent metadata only. Must not lock OS files, store source contents, mutate source files, create branches/worktrees, call providers, expose secrets/env values, or weaken policy gates. |
| CommandExecutionResultRepository | `InMemoryCommandExecutionResultRepository` in `packages/runner` | persistent_recommended before wider local runner use | `command_execution_results` | `id`, `agent_run_id`, `task_id`, `task_run_id`, `status`, `created_at` | Retain bounded previews with AgentRun history | Store size-limited sanitized previews only; blocked/timeout reasons should be auditable. |

## Phase 2

| Repository or Store | Current Implementation | Priority | Recommended Table | Key Indexes | Retention | Audit Requirements |
|---|---|---:|---|---|---|---|
| BranchLeaseRepository | `InMemoryAichestraStore`; opt-in `PostgresAichestraStore` | persistent_required | `branch_leases` | `id`, `repo_id`, `task_run_id`, `status`, `expires_at` | Until task/PR archival plus audit retention | Lease create/release/expire must be auditable. |
| ConflictRiskRepository | Derived by `computeRepoConflictRisks` | persistent_recommended | `conflict_risks` | `repo_id`, `source_task_run_id`, `target_task_run_id`, `risk_score` | Can be recomputed if leases and simulations persist | Store snapshots when used for queue decisions. |
| MergeQueueRepository | `InMemoryAichestraStore`; opt-in `PostgresAichestraStore` | persistent_required | `merge_queue_entries` | `id`, `repo_id`, `status`, `priority`, `task_run_id` | Until PR/task archival | Queue status transitions must be auditable. |
| MergeQueuePolicyDecisionRepository | `MergeQueuePolicyService` in-memory decision list over existing merge queue entries | persistent_recommended before production merge governance | `merge_readiness_decisions` | `id`, `queue_entry_id`, `repo_id`, `decision`, `created_at`, `request_id`, `correlation_id` | Retain with queue entry and approval evidence | Stores metadata-only readiness decisions, reasons, warnings, required actions, priority, and sanitized actor/service-account attribution. Must not execute merges, store secrets/env values, or imply auto-merge approval. |
| MergeQueueHoldRepository | `MergeQueuePolicyService` in-memory hold list over existing merge queue entries | persistent_recommended before production merge governance | `merge_queue_holds` | `id`, `queue_entry_id`, `repo_id`, `hold_kind`, `severity`, `created_at`, `released_at` | Retain with queue entry and review evidence | Stores metadata-only holds and release metadata. Hold/release actions must be auditable and must not mutate Git, update provider PRs, delete branches, or bypass policy gates. |
| MergeSimulationResultRepository | `InMemoryAichestraStore`; opt-in `PostgresAichestraStore` | persistent_required | `merge_simulation_results` | `id`, `repo_id`, `task_run_id`, `branch_lease_id`, `status` | Long-lived conflict evidence | Simulation command metadata must remain sanitized. |

## Phase 3

| Repository or Store | Current Implementation | Priority | Recommended Table | Key Indexes | Retention | Audit Requirements |
|---|---|---:|---|---|---|---|
| SkillRegistryRepository | `InMemoryAichestraStore`; file-backed local repo; opt-in `PostgresAichestraStore` | persistent_required | `skills` | `id`, `(name, version)`, `status`, `approval_status`, `eval_status` | Registry lifetime | Create/update/status/approval/eval changes require audit and revision. |
| HarnessRegistryRepository | Same as Skill | persistent_required | `harnesses` | `id`, `(name, version)`, `status`, `runtime_type` | Registry lifetime | Same as Skill. |
| InstructionRegistryRepository | Same as Skill | persistent_required | `instructions` | `id`, `(name, version)`, `checksum_status`, `scope` | Registry lifetime | Checksum verification and status changes require audit and revision. |
| RegistryAuditRepository | In-memory by default; opt-in Postgres | persistent_required | `registry_audit_logs` | `id`, `target_kind`, `target_id`, `actor_id`, `created_at` | Long-lived or compliance retention | Append-only. |
| RegistryHistoryRepository | In-memory by default; opt-in Postgres | persistent_required | `registry_revisions` | `id`, `target_kind`, `target_id`, `revision_number` | Registry lifetime | Append-only rollback source. |
| RegistryPackageRepository | In-memory/file-backed local; opt-in Postgres | persistent_required | `registry_packages` | `id`, `(name, version)`, `package_kind` | Registry package lifetime | Import/export and package creation should be audited. |
| RegistryEvalResultRepository | In-memory by default; opt-in Postgres | persistent_required | `registry_eval_results` | `id`, `target_kind`, `target_id`, `status`, `attached_at` | Long-lived evidence retention | Attachment and status update audit. |
| ApprovalQueue read model | Derived in `RegistryService.listApprovalQueue` | ephemeral_ok | `registry_approval_queue_view` | `approval_status`, `target_kind`, `owner` | Rebuild from registry tables | No separate audit; source mutations are audited. |

## Phase 4

| Repository or Store | Current Implementation | Priority | Recommended Table | Key Indexes | Retention | Audit Requirements |
|---|---|---:|---|---|---|---|
| FailureSignalRepository | `InMemoryImprovementRepository` | persistent_required | `failure_signals` | `id`, `source_type`, `source_id`, `task_run_id`, `target_kind`, `category` | Retain for improvement analysis window | Manual signal creation should be auditable later. |
| FailureClusterRepository | `InMemoryImprovementRepository` | persistent_required | `failure_clusters` | `id`, `category`, `target_kind`, `target_ref`, `status` | Retain while proposals reference clusters | Status changes should be audited. |
| ImprovementCandidateRepository | `InMemoryImprovementRepository` | persistent_required | `improvement_candidates` | `id`, `source_cluster_id`, `target_kind`, `target_id`, `status` | Retain while proposals exist | Triage decisions should be auditable. |
| ImprovementProposalRepository | `InMemoryImprovementRepository` | persistent_required | `improvement_proposals` | `id`, `candidate_id`, `target_kind`, `target_id`, `status` | Long-lived governance evidence | Status changes and decisions require audit. |
| DraftRegistryChangeRepository | `InMemoryImprovementRepository` | persistent_required | `draft_registry_changes` | `id`, `proposal_id`, `target_kind`, `target_id`, `status` | Retain with proposal history | Must prove no active mutation occurred. |
| ProposalReadinessRepository | `InMemoryImprovementRepository` | persistent_recommended | `proposal_readiness` | `proposal_id`, `evaluated_at` | Latest plus history if needed | Apply gate decisions should reference readiness. |
| ProposalGovernanceDecisionRepository | `InMemoryImprovementRepository` | persistent_required | `proposal_governance_decisions` | `id`, `proposal_id`, `actor_id`, `decision`, `created_at` | Long-lived governance retention | Append-only. |
| ProposalEvalRunRepository | `InMemoryImprovementRepository` | persistent_required | `proposal_eval_runs` | `id`, `proposal_id`, `eval_requirement_id`, `status` | Long-lived evidence retention | Attachment must be audited. |
| CanaryReadinessRepository | `InMemoryImprovementRepository` | persistent_recommended | `canary_readiness` | `proposal_id`, `evaluated_at` | Latest plus optional history | Readiness checks should be auditable. |
| ProposalApplyGateRepository | `InMemoryImprovementRepository` | persistent_required before apply | `proposal_apply_gates` | `proposal_id`, `can_apply`, `evaluated_at` | Long-lived for apply governance | Apply-gate and blocked apply attempts must be audited. |
| ImprovementGovernanceAuditRepository | `InMemoryImprovementRepository` | persistent_required | `improvement_governance_audit_events` | `id`, `proposal_id`, `action`, `actor_id`, `created_at` | Long-lived or compliance retention | Append-only. |

## Secrets and Sandbox

| Repository or Store | Current Implementation | Priority | Recommended Table | Key Indexes | Retention | Audit Requirements |
|---|---|---|---|---|---|---|
| SecretRefRepository | `InMemorySecretRefRepository` in `packages/security`; extended by SecretRef-backed Provider Credentials v1 | persistent_required before real secret backend integration | `secret_refs` | `id`, `provider`, `secret_kind`, `scope`, `status`, `env_key` | Secret reference lifecycle | Must never store raw secret values, env var values, or credential cache paths. `env_key` is a reference only. GitHub token, GitHub webhook secret, LLM API key, and provider API key SecretRefs remain metadata-only. |
| SecretScopeRepository | `InMemorySecretScopeRepository` | persistent_required before production secret leasing | `secret_scopes` | `id`, `requires_approval` | Scope/policy lifecycle | Scope changes must be audited before production. |
| SecretLeaseRepository | `InMemorySecretLeaseRepository` | persistent_required before any real secret issuance | `secret_leases` | `id`, `secret_ref_id`, `scope_id`, `task_id`, `task_run_id`, `status`, `expires_at` | Lease lifecycle plus compliance retention | Lease requests, denials, metadata credential handle issuance, expiration, and revocation must be audited without secret values. |
| SecretAccessDecisionRepository | `InMemorySecretAccessDecisionRepository` | persistent_required before real secret issuance | `secret_access_decisions` | `id`, `secret_ref_id`, `scope_id`, `actor_id`, `task_id`, `task_run_id`, `created_at` | Compliance/audit retention | Decision records must link to policy decisions and never contain raw secrets. |
| SandboxProfileRepository | `InMemorySandboxProfileRepository` | persistent_required before production sandbox configuration | `sandbox_profiles` | `id`, `kind`, `status` | Sandbox profile lifecycle | Profile changes require review/audit. Future container/Firecracker/Kubernetes profiles remain disabled placeholders. |
| SandboxSessionRepository | `InMemorySandboxSessionRepository` | persistent_recommended before broader local runner use | `sandbox_sessions` | `id`, `profile_id`, `task_id`, `task_run_id`, `runner_kind`, `status`, `created_at` | Retain with AgentRun history | Session create/complete/cleanup must be audited. |
| SandboxDecisionRepository | `InMemorySandboxDecisionRepository` | persistent_required before production sandbox use | `sandbox_decisions` | `id`, `profile_id`, `task_id`, `task_run_id`, `actor_id`, `created_at` | Compliance/audit retention | Decisions should link to policy decisions. |
| NetworkEgressPolicyRepository | `InMemoryNetworkEgressPolicyRepository` | persistent_required before real network egress controls | `network_egress_policies` | `id`, `status`, `default_action` | Network policy lifecycle | Policy changes and blocked egress decisions require audit. |
| RedactionPolicyRepository | `InMemoryRedactionPolicyRepository` | persistent_required before real provider output retention | `redaction_policies` | `id`, `status`, `retention_class` | Redaction policy lifecycle | Policy changes require audit/review before production. |
| SecurityAuditRepository | `InMemorySecurityAuditRepository` | persistent_required before real secrets/sandbox/provider integrations | `security_audit_events` | `id`, `event_type`, `target_kind`, `task_id`, `task_run_id`, `actor_id`, `created_at` | Long-lived compliance retention | Append-only; metadata must be sanitized and must not contain secrets, tokens, credential cache contents, or raw prompts. |
| CredentialHandleRepository | Not persisted in v1; handles are returned as metadata-only resolution results | persistent_recommended before production credential resolution | `credential_handles` future table | `id`, `secret_ref_id`, `secret_kind`, `status`, `expires_at` | Short-lived resolution evidence | Must never store raw credential values; link to auth, policy, and audit events only. |

## Test Infrastructure

| Repository or Store | Current Implementation | Priority | Recommended Table | Key Indexes | Retention | Audit Requirements |
|---|---|---:|---|---|---|---|
| In-memory storage provider | `createInMemoryStorageProvider` | test_only | none | none | Test lifetime | None. |
| Persistent storage provider placeholder | `createPersistentStorageProviderPlaceholder` | test_only | none | none | Not runtime storage | Throws clear not-implemented errors. |
| Postgres storage provider | `createPostgresStorageProvider` | persistent_required for integration environments | Postgres schema in `infra/migrations/0001_initial_aichestra_schema.sql` | Repository-specific indexes above | Environment lifetime | Health check and contract tests are opt-in. |
