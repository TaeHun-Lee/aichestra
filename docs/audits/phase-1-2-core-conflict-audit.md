# Aichestra — Phase 1 / Phase 2 / Real Git Adapter Core & Conflict Audit

> **Audit date**: 2026-05-14
> **Reviewer**: Claude (claude-opus-4-7, 1M context)
> **Working tree**: clean at `main` @ `61a4036 feat: Secret Backend Migration Planning v0`
> **Scope**: audit-only review of Phase 1 (core task + vertical slice), Phase 2 (Conflict Manager v0/v1), and Real Git Adapter v0/v1/v2 — restricted to the file set the user requested.

---

## 1. Phase 1 Audit

| # | Surface | Status | Evidence | Concerns |
|---|---|---|---|---|
| 1.1 | **Task domain model** | implemented | `packages/core/src/domain/models.ts:99–117` — `Task` carries `id`, `title`, `description`, `status`, `requesterUserId`, `repoId`, `baseBranch`, `branchName`, `selectedAgent`, `selectedModel`, `selectedSkillIds`, `selectedHarnessId`, `instructionSetId`, `budgetLimitUsd`, `conflictRiskScore`, `createdAt`, `updatedAt`. Status enum + transitions in `domain/status.ts:1–55`. Schema validation in `core/src/schemas/{domain,schema}.ts`. | None. State enum is broad (19 values); future hardening should split visible UI states from internal-only transitions, but not a Phase-1 blocker. |
| 1.2 | **TaskRun domain model** | implemented | `models.ts:119–146` — `TaskRun` carries `taskId`, `attempt`, `status`, `agent`, `model`, `modelProvider`, registry refs (`selectedSkillRefs`, `selectedHarnessRef`, `selectedInstructionRefs`), resolution warnings/errors, `instructionSetId`, `changedFiles`, `diffSummary`, `pullRequestUrl`, `errorMessage`, timestamps. `TaskRunStatus = queued | running | succeeded | failed | cancelled` (`status.ts:25–33`). | Schema separation between selected ref *id/version* and *kind* is consistent; no concern. |
| 1.3 | **Task creation API** | implemented | `apps/api/src/main.ts:3247–3249` — `POST /tasks` → `store.createTask(...)` returns `taskView(task)` at 201. Schema coercion in `packages/core/src/schemas/domain.ts:42`. Demo create-payload exercised in `tests/api-health.test.ts:160–183`. | None. |
| 1.4 | **Task run trigger API** | implemented | `apps/api/src/main.ts:3268–3279` — `POST /tasks/:id/run` calls `runAgentTaskWorkflow(task.id, { store })` and returns `task`, `result`, `taskRuns`, `pullRequests`, `usageEvents` at 200. Repeated-run 409 is enforced via `ConflictError` from `apps/worker/src/workflows/run-agent-task-workflow.ts:107–110`, mapped to HTTP 409 in `main.ts:4117–4123`. Verified by `tests/api-health.test.ts:131–157`. | None. (The separate `POST /tasks/:id/run-agent` route is a different path that exercises `AgentRunnerService` for Local Agent Runner v1 — it pre-checks active runs at `main.ts:3281–3285` with the same 409 semantics.) |
| 1.5 | **Worker/service execution path** | implemented | `apps/worker/src/workflows/run-agent-task-workflow.ts:80–370` orchestrates: policy → registry resolution → planning/queued/branch_created/running → branch → instruction-set → agent run → usage ledger → branch lease → merge simulation → conflict risk → testing → test runner → PR create → merge queue entry → final state. Modular deps fall back to mock implementations (`MockGitProvider`, `MockAgentRunner`, `MockTestRunner`, `MockMergeSimulator`, default LLM gateway, `MockPolicyEngine`, in-memory store/usage). | The worker app entrypoint (`apps/worker/src/main.ts`) is a 17-line stub fixture (no Temporal scheduler) — fine for the MVP scaffold but means production runtime orchestration is still a deferred track. |
| 1.6 | **Mock policy check** | implemented | `MockPolicyEngine` from `@aichestra/policy` (legacy) is the default; new `PolicyService` + `StaticPolicyEngine` already drives the rest of the API. Workflow path: `policyEngine.evaluateTask({ taskId, files, budgetLimitUsd })` (`run-agent-task-workflow.ts:137–155`); on deny, transitions to `policy_blocked` and appends `policy.blocked` audit. `policy_blocked` transition handled in `transitionToPolicyBlocked` (`workflow.ts:53–62`). | The workflow still uses the legacy `MockPolicyEngine`; the modern `StaticPolicyEngine` is wired at the API layer. Not a Phase-1 blocker (the legacy path is mock-only and harmless) but worth aligning to a single policy service in a future refactor. |
| 1.7 | **Model/skill/harness/instruction selection** | implemented | `registryService.resolveRegistryContextForTask({ task, agent, repo })` (`workflow.ts:114–118`) returns `selectedSkills/selectedHarness/selectedInstructions` + warnings/errors; persisted onto `TaskRun.selectedSkillRefs / selectedHarnessRef / selectedInstructionRefs / registryResolutionWarnings / registryResolutionErrors` (`workflow.ts:182–198`). Model selection through `CatalogModelRouter` (`workflow.ts:130–135`). Asserted in `tests/mock-workflow-vertical-slice.test.ts:32–37` and `tests/api-health.test.ts:191–250`. | None. |
| 1.8 | **Branch creation** | implemented | `branchName = \`ai/${task.id}/${agent}/${slugify(task.title)}\`` (`workflow.ts:170–175`). `gitProvider.createBranch(...)` → `store.updateTask({ branchName })` → transition `branch_created`. MockGitProvider returns deterministic branch metadata; `BranchLease` written downstream. | Branch naming is structurally `ai/...`; matches the GitHub allowed prefix gate in `GitHubGitProvider` (`allowedBranchPrefix = "ai/"`, `github-git-provider.ts:116`). |
| 1.9 | **Mock agent execution** | implemented | `MockAgentRunner.run({ taskId, taskRunId, branch, agent, model, prompt, skillVersions, harnessVersion, instructionSet })` (`workflow.ts:214–226`) returns `changedFiles`, `diffSummary`, `summary`, `testsPassed`, and `usage`. Prompt is sanitized + wrapped in `AICHESTRA_USER_TASK_INPUT_START/END` markers (`workflow.ts:64–78`) — a real defense-in-depth against prompt injection from task descriptions. | None. |
| 1.10 | **Diff summary** | implemented | `agentRun.diffSummary` recorded onto `TaskRun.diffSummary` for both failure (`workflow.ts:281–290`) and success (`workflow.ts:346–354`) paths; surfaced in API responses (`apps/api/src/main.ts:3259–3266`, dashboard read-model). Verified `2 files changed, 18 insertions, 4 deletions` in `tests/mock-workflow-vertical-slice.test.ts:46` and `tests/api-health.test.ts:182`. | None. |
| 1.11 | **Mock test result** | implemented | `MockTestRunner.run({ taskId, commands, changedFiles, prompt })` (`workflow.ts:274–279`). On `!agentRun.testsPassed || !testResult.passed` the workflow transitions to `failed` and records `testResult.output` as `errorMessage`. | None. |
| 1.12 | **PR creation** | implemented | `gitProvider.createDraftPullRequest({ taskId, repoId, provider: "mock", branchName, baseBranch, title })` → `store.createPullRequest({...})`; URL surfaces in `TaskRun.pullRequestUrl` and dashboard (`workflow.ts:303–318`). URL format `mock://pull-requests/...` is asserted in tests. | None. |
| 1.13 | **Usage ledger entry** | implemented | `usageLedger.record({ ...agentRun.usage, metadata: { model_provider, model_selection_reason, skill_id, harness_id, selected_*_refs, instruction_set_id, instruction_set_hash, registry_resolution_warnings, registry_resolution_errors } })` (`workflow.ts:227–243`). Returned `usageEvent.id` is part of `RunAgentTaskWorkflowResult.usageEventIds`. Visible via `GET /usage?taskId=...`. | None. |
| 1.14 | **taskRunId/taskId attribution** | implemented | `taskId`/`taskRunId` are required on usage events (`db/src/usage-ledger.ts`), branch leases (`workflow.ts:244–255`), merge simulations (`workflow.ts:256–266`), merge queue entries (`workflow.ts:321–337`), PRs (`store.createPullRequest`). Filter endpoints exist: `GET /usage?taskId=`, `GET /tasks/:id/runs`, `GET /branches/leases?repoId&status`, etc. | None. |
| 1.15 | **Dashboard / API visibility** | implemented | `apps/api/src/dashboard-read-model.ts:1–910` aggregates per-domain read models; `/dashboard/tasks`, `/dashboard/git`, `/dashboard/conflicts`, `/dashboard/overview` consume the same store. `apps/web/lib/dashboard-data-provider.ts` switches between API and explicit demo fallback. Sanitization is enforced via `packages/shared/src/dashboard-read-models.ts:334–335` (token-like / credential-cache patterns). | None. |
| 1.16 | **Tests** | implemented | Phase 1 coverage: `tests/task-state-machine.test.ts` (3 cases, state transitions / rerun); `tests/mock-workflow-vertical-slice.test.ts` (full slice, blocked overlap, conflict signaling); `tests/api-health.test.ts:116–250` (health, 409 on active run, full create+run, registry endpoints); `tests/instruction-resolver.test.ts`, `tests/mock-llm-gateway.test.ts`. All currently green (3 consecutive `pnpm test` runs of 228/0/4). | None. The previously observed flake in `local-agent-protocol-v1.test.ts:288` did not reproduce on three subsequent runs — leave a follow-up to investigate test-isolation, but it does not affect Phase 1. |

**Phase 1 verdict: implemented (complete for current milestone).**

---

## 2. Phase 2 Audit

| # | Surface | Status | Evidence | Concerns |
|---|---|---|---|---|
| 2.1 | **BranchLease** | implemented | `packages/core/src/domain/models.ts` (`BranchLease` type with `taskId`, `taskRunId`, `repoId`, `branchId`, `branchName`, `baseBranch`, `files`, `symbols`, `tests`, `status: "active" | "released" | "expired"`). Lifecycle in `packages/db/src/repository.ts` (`createBranchLease`, `listBranchLeases`). Wired from `workflow.ts:244–255`. API: `GET /branches/leases?repoId&status` (`apps/api/src/main.ts:3387–3394`). | None. |
| 2.2 | **ConflictRisk** | implemented | `domain/models.ts:160–176` — `ConflictRisk` with `sourceLeaseId/targetLeaseId`, `sourceTaskRunId/targetTaskRunId`, `overlapFiles`, `riskScore`, `riskLevel`, `reasons`, `recommendation`, `simulationStatus`, `simulationRiskContribution`, `simulationSummary`, `computedAt`. Constructed in `core/src/conflicts/scoring.ts:207–234`. API: `GET /conflicts/risks?repoId&taskRunId` (`main.ts:3396–3406`). | None. |
| 2.3 | **MergeQueueEntry** | implemented | Stored & queried via `store.createMergeQueueEntry/listMergeQueueEntries/markMergeQueueEntryMerged/cancelMergeQueueEntry`. API: `GET /merge-queue?repoId`, `POST /merge-queue/:id/mark-merged`, `POST /merge-queue/:id/cancel` (`main.ts:3471–3490`). Worker builds the entry from `mergeQueueFieldsForLease` (`workflow.ts:321–337`). | None. |
| 2.4 | **MergeSimulationResult** | implemented | `domain/models.ts` `MergeSimulationResult` with `mode`, `status`, `conflictingFiles`, `changedFiles`, `summary`, `rawCommandMetadata`, `riskContribution`. Persisted via `store.recordMergeSimulation`; API: `GET/POST /merge-simulations` (`main.ts:3408–3469`). | None. |
| 2.5 | **File-overlap scoring** | implemented | `packages/core/src/conflicts/scoring.ts:126–175` — `computePairRiskScore` classifies overlap files into `critical / package / source / test / docs`, raising `riskScore` to 0.9/0.75/0.6/0.3/0.1 respectively. `sortedOverlap` (sorted intersection of normalized paths); `sameDirectoryRisk` heuristic (top-level dir with ≥3 combined files) ⇒ 0.35. `recommendationForRisk` adjusts `safe/monitor/serialize/block/human_review`. Verified by `tests/conflict-risk-scoring.test.ts:25–110` and `tests/mock-git-conflict-risk.test.ts`. | None. |
| 2.6 | **LocalGitDryRunMergeSimulator** | implemented (local-only) | `packages/adapters/src/git/merge-simulators.ts:117–193`. Runs `git -C <repoPath> rev-parse --is-inside-work-tree`, then `git diff --name-only`, then `git merge-tree --write-tree <targetRef> <sourceRef>`. Exit-code 0 ⇒ `clean`, conflict markers in output ⇒ `text_conflict`, otherwise ⇒ `failed`; missing `repoPath` ⇒ `unavailable`. Output is `sanitizeOutput`'d (CR-stripped, 2000-byte cap, `repoPath` redacted to `<repoPath>`). Verified by `tests/merge-simulators.test.ts:42–130`. **API caller validates the local path against the `AICHESTRA_ALLOWED_REPO_PATHS` allowlist** (`main.ts:3440–3443`). | None. The simulator runs `git -C ...` against caller-supplied paths only, never fetches/pushes, never mutates the working branch — compliant with AGENTS.md rule 53. |
| 2.7 | **clean/text_conflict/failed/unavailable statuses** | implemented | All four statuses produced by `MockMergeSimulator` and `LocalGitDryRunMergeSimulator`. `riskContributionForSimulationStatus` maps statuses to 0.1/0.8/0.35/0.35/0 (`scoring.ts:95–100`). API endpoint validates status enum (`main.ts:363–365, 3454–3458`). | None. |
| 2.8 | **Merge queue recommendation** | implemented | `mergeQueueDecision(riskScore, simulation)` (`scoring.ts:236–291`) maps risk + simulation status to `{ status: ready|blocked, reasons, blockingReasons, recommendation: safe_to_queue | ready_for_review | requires_rebase | conflict_detected | manual_review_required | simulation_unavailable }`. Asserted by `tests/conflict-risk-scoring.test.ts:115–187` and `tests/mock-workflow-vertical-slice.test.ts`. | None. |
| 2.9 | **Changed-files linkage** | implemented | `agentRun.changedFiles` → `TaskRun.changedFiles` → `BranchLease.files` → conflict scoring. PR sync paths refresh changed files (`apps/api/src/main.ts:3660–3681`, `git-adapter/src/webhooks.ts` PR sync flow). GitHub changed-files fetched only through gated `client.getPullRequestChangedFiles` and `sanitizeChangedFile`. | None. |
| 2.10 | **PR/branch sync read models** | implemented | `GitPullRequestSyncState`, `GitBranchSyncState` in `core/src/domain/models.ts`; store helpers `upsertGitPullRequestSyncState`, `upsertGitBranchSyncState`, `listGitPullRequestSyncStates`, `listGitBranchSyncStates` (`db/src/repository.ts:432+`). Receiver/sync service updates both non-destructively (`packages/git-adapter/src/webhooks.ts`). API: `/git/repos/:repoId/{pr-sync,branch-sync}` and manual `POST .../pull-requests/:n/sync` and `.../refresh-changed-files` (`main.ts:3590–3669`). | None. |
| 2.11 | **No automatic merge** | implemented (structurally unsupported) | `remoteMergeEnabled` is hard-coded `false` in all config views (`provider-factory.ts:75`, `github-git-provider.ts:131`, `service.ts:100/140/700/834`, `adapters/interfaces.ts:166`, `shared/dashboard-read-models.ts:24`). `AICHESTRA_ALLOW_REMOTE_MERGE` is **never read** in the runtime. The only operation that touches "merged" state is `POST /merge-queue/:id/mark-merged` which only mutates the local merge-queue record — it does not call a provider. | None. |
| 2.12 | **No rebase / force-push / branch-delete** | implemented | No code path calls `git push --force`, `git rebase`, `git branch -D`, or a provider equivalent. Default rules deny these via the runner harness (`packages/runner/src/harness-policy.ts:25–28`) and policy engine (`packages/policy/src/default-rules.ts:652+`). GitHub provider does not expose merge/rebase/force-push methods. `tests/real-git-adapter-v1.test.ts:281–294` ("Remote merge and rebase remain unsupported and policy-audited") locks this in. | None. |
| 2.13 | **Tests** | implemented | `tests/conflict-risk-scoring.test.ts` (13 cases: no overlap, docs-only, test-only, source, package, critical, same-directory, pair-order, merge queue decision, dry-run combinations); `tests/mock-git-conflict-risk.test.ts`; `tests/merge-simulators.test.ts` (5 cases including local fixture clean + text_conflict + unavailable); `tests/mock-workflow-vertical-slice.test.ts` (vertical slice + blocked overlap); `tests/api-health.test.ts:365–...` (active leases / conflict risks / merge queue actions). All green. | None. |

**Phase 2 verdict: implemented (complete for current milestone).**

---

## 3. Real Git Adapter v0 / v1 / v2 Audit

| # | Surface | Status | Evidence | Concerns |
|---|---|---|---|---|
| 3.1 | **`MockGitProvider` default** | implemented | `provider-factory.ts:128` returns `new MockGitProvider()` when the env provider kind is anything but `local` or `github`. Default env (`AICHESTRA_GIT_PROVIDER` unset) ⇒ `providerKind = "mock"`. Worker workflow also instantiates `new MockGitProvider()` as the deps fallback (`workflow.ts:82`). Tests: `real-git-adapter-v0.test.ts:86–110`. | None. |
| 3.2 | **`LocalGitProvider` local-only** | implemented | `packages/adapters/src/git/local-git-provider.ts:80–99` — `git -C <repoPath> ...` only; refuses unsafe paths via `isUnsafePath` (`local-git-provider.ts:75–78`). `LocalGitProvider` is instantiated only when `AICHESTRA_GIT_PROVIDER=local` (`provider-factory.ts:100–104`). No `git fetch`/`push`/`merge`/`rebase` in the local provider. Verified by `tests/real-git-adapter-v0.test.ts:112–151`. | None. |
| 3.3 | **`GitHubGitProvider` gated** | implemented | `provider-factory.ts:106–126` builds the GitHub provider, but the `FetchGitHubClient` is **only constructed when `config.remoteGitEnabled && token && allowedRepos.length > 0`** (line 111–113). Otherwise the provider falls through to `NoopGitHubClient`. Every read/write method re-checks `remoteGitEnabled` / `remoteBranchCreateEnabled` / `remotePullRequestCreateEnabled` / `configured` / repo allowlist / branch prefix at request time (`github-git-provider.ts:368–409`). | None. |
| 3.4 | **`GitHubClient` boundary** | implemented | `packages/adapters/src/git/github-client.ts:149–167` — single `fetch(url, ...)` site with `Authorization: Bearer ${this.token}`, `x-github-api-version: 2022-11-28`, `user-agent: aichestra-real-git-adapter-v1`. No SDK import. Errors return `github_http_<status>`; 204 returns `{}`. `sanitizePath`/`sanitizeChangedFile` strip control chars and cap path length. | None. |
| 3.5 | **Branch creation gates** | implemented | `validateWrite` (`github-git-provider.ts:377–392`) checks: `remoteGitEnabled` → `remoteBranchCreateEnabled` → `configured` (token) → `resolveTarget` (owner/repo present) → `repoAllowed(slug)` → `branchName.startsWith(allowedBranchPrefix)`. Audit on `branch_create_blocked` includes the failing reason without secrets (`service.ts:351–361`). Asserted by `tests/real-git-adapter-v1.test.ts:190–219`. | None. |
| 3.6 | **PR creation gates** | implemented | `validatePullRequest` (`github-git-provider.ts:394–409`) checks: `remoteGitEnabled` → `remotePullRequestCreateEnabled` → `configured` → target resolved → repo allowlisted → branch prefix. Asserted in v1 and v2 tests. Title/body sanitized via `sanitizeText` (240 / 4000-char cap, ghp_/github_pat_/Bearer redacted) before being sent to GitHub. | None. |
| 3.7 | **Webhook gates** | implemented | `github-webhooks.ts:101–120` — config requires `webhooksEnabled` truthy + `webhookSecretConfigured`. Receiver (`git-adapter/src/webhooks.ts:121–139`) returns 409/blocked when disabled or secret missing. `webhookAcceptUnverified` defaults `false`; unverified payloads return 401 (`webhooks.ts:248–256`). Repo allowlist enforced (`webhooks.ts:191–203` → 403). | None. |
| 3.8 | **Webhook signature verification boundary** | implemented | Three verifier kinds in `adapters/src/git/github-webhooks.ts`: `NoopGitHubWebhookVerifier` (disabled/default), `MockGitHubWebhookVerifier` (deterministic test fixtures), `HmacGitHubWebhookVerifier` (sha256 HMAC + **constant-time** comparison via `timingSafeEqual`, line 263–272). Algorithm chosen by `createGitHubWebhookRuntimeFromEnv` based on enabled+secret presence (`github-webhooks.ts:135–141`). Asserted by `tests/real-git-adapter-v2.test.ts:296–319`. | None. |
| 3.9 | **Replay / dedupe / dead-letter** | partial (**planning only at runtime**) | The runtime receiver records `deliveryId` and `payloadHash` for every event (`git-adapter/src/webhooks.ts:116–119`) but **does not deduplicate by `deliveryId`** at `store.recordGitWebhookEvent` (`db/src/repository.ts:398–406`). Replay protection / dead-letter classification exists as **planning-only metadata** in `packages/deployment-readiness/src/{types,catalog,service}.ts` (`GitHubWebhookReplayStatus`, `GitHubWebhookProcessingStatus`, `replayRejectedBehavior`, seed catalogs at `catalog.ts:885–955`). Exposed via `/readiness/github-app/replay-protection` and `/readiness/github-app/dead-letter` — read-only. | **Production-readiness follow-up**: implement actual duplicate-delivery rejection (and replay-rejected metric) in `git-adapter/src/webhooks.ts` before enabling production webhook traffic, per `docs/roadmaps/github-app-production-webhook-hardening/`. Not a blocker for v2's planning scope. |
| 3.10 | **PR/branch sync read model** | implemented | See 2.10. `manualSyncPullRequest` / `refreshChangedFiles` go through `GitHubSyncService` (`webhooks.ts:327–333`); policy decisions logged; provider gates re-checked. Branch sync from push webhook updates branch metadata non-destructively. Asserted by `tests/real-git-adapter-v2.test.ts:350–451`. | None. |
| 3.11 | **Policy integration** | implemented | Webhook receiver evaluates `git.webhook.verify`, `git.webhook.receive`, `git.webhook.process` policies before dispatching (`git-adapter/src/webhooks.ts:225–292`); when policy denies, event status becomes `rejected` and HTTP 403/401 is returned. Git integration service evaluates `git.pull_request.create` and other remote operations via `evaluateRemotePolicy` (`service.ts:400–448`). Default rules deny remote-git operations without gates (`packages/policy/src/default-rules.ts:652+`). Asserted by `tests/real-git-adapter-v2.test.ts:453–485` ("policy defaults deny unverified webhooks, merge, and rebase while allowing verified read-model sync"). | None. |
| 3.12 | **SecretRef integration** | implemented | `provider-factory.ts:164–209` resolves GitHub token via `options.credentialResolver({ secretRefId, purpose: "github_api_call", providerId: "github", policyContext })` first; SecretRef failure short-circuits without falling through to legacy env. Webhook secret via the symmetric `secretResolver` path in `github-webhooks.ts:143–187`. Legacy env (`AICHESTRA_GITHUB_TOKEN`, `AICHESTRA_GITHUB_WEBHOOK_SECRET`) is permitted only when no `_SECRET_REF` is set, and is audited through `legacyCredentialFallbackAuditor` / `legacySecretFallbackAuditor` (wired in `apps/api/src/main.ts:4159–4210` via `securityService.recordSecretAudit`). | **Minor follow-up** (carried over from the SecretRef v1 completion review): no direct negative test asserting `disabled/revoked` SecretRef does not silently fall back to legacy env. |
| 3.13 | **Audit / redaction** | implemented | `GitIntegrationService.sanitizeMetadata` (`service.ts:892–916`) replaces values for token/secret/credential/authorization/api-key keys with `[redacted]`, and `sanitizeText` strips `ghp_*`/`github_pat_*`/`Bearer ...` patterns. `GitHubGitProvider.sanitizeText` (`github-git-provider.ts:469–478`) applies the same redaction to PR titles/bodies and error messages before they reach audit metadata. Webhook audit records `payloadHash` instead of raw bodies. | None. |
| 3.14 | **No secret exposure** | implemented | `getConfig()` returns `githubCredentialSource`, `githubCredentialStatus`, `githubCredentialReason`, plus boolean `githubConfigured` — never the token. Webhook config returns `webhookSecretSource`, `webhookSecretStatus`, `webhookSecretReason`, `webhookSecretConfigured` — never the secret. Asserted by `tests/real-git-adapter-v2.test.ts:487–507` ("GitHub webhook API is disabled by default and exposes no secrets in health, config, or dashboard"). Dashboard sanitizer in `packages/shared/src/dashboard-read-models.ts:334–335` catches the same patterns again as defense-in-depth. | None. |
| 3.15 | **Default tests do not call GitHub** | implemented | All v0/v1/v2 tests use either `FixtureGitHubClient` or mock verifiers (`tests/real-git-adapter-v1.test.ts:30+`, `tests/real-git-adapter-v2.test.ts:30+`). The optional remote integration test is `test(..., { skip: ... })` keyed on `AICHESTRA_GITHUB_INTEGRATION_TESTS=true` (`v1.test.ts:345`, `v2.test.ts:547`). `pnpm test` skips these unless the env vars are set — confirmed by the validation run (4 skipped). `scripts/lint.mjs:37–39` enforces the `fetch("http...")` ban for the rest of the tree. | None. |

**Real Git Adapter verdict: v2 implemented (gated), with one Phase-2-level production follow-up on runtime dedupe/replay protection.**

---

## 4. Safety Audit

| Control | Verdict | Evidence |
|---|---|---|
| **No auto-merge** | pass | `remoteMergeEnabled = false` hard-coded across `provider-factory.ts:75`, `github-git-provider.ts:131`, `service.ts:100,140,700,834`, `interfaces.ts:166`, `shared/dashboard-read-models.ts:24`. `AICHESTRA_ALLOW_REMOTE_MERGE` is **never read** in code. `/merge-queue/:id/mark-merged` only updates the in-memory MergeQueueEntry; it does not call any provider. |
| **No force-push** | pass | No code path invokes `--force` / `--force-with-lease`. Disabled via runner harness policy (`packages/runner/src/harness-policy.ts:25–28` denies `git push`) and policy engine defaults (`policy/src/default-rules.ts:652+`). Deployment-readiness catalog marks `forcePushAllowed: false`. |
| **No branch deletion** | pass | Neither `GitHubGitProvider`, `LocalGitProvider`, nor `MockGitProvider` exposes a `deleteBranch` method. Runner deny list blocks `rm` and `git push`. Push webhook handler updates metadata only (`webhooks.ts:407–420`-class flow). |
| **No remote call without explicit env gates** | pass | The two real-network sites — `packages/adapters/src/git/github-client.ts:151` (GitHub REST) and `packages/llm-gateway/src/providers.ts:139` (OpenAI-compatible) — both require their owning provider to pass an explicit env gate at construction *and* at every request. `NoopGitHubClient` is the default. Default `pnpm test` makes 0 outbound HTTP calls (confirmed via 3 consecutive runs). |
| **Token not returned in health / API / dashboard / audit** | pass | `getConfig()` returns only `githubCredentialSource/Status/Reason` + `githubConfigured: boolean` (`service.ts:94–114`). Webhook config returns only `webhookSecretSource/Status/Reason` + `webhookSecretConfigured: boolean` (`github-webhooks.ts:128–134`). Provider audit redaction strips `ghp_`, `github_pat_`, `Bearer ...` and any field named `token/secret/credential/authorization/api[_-]?key` (`service.ts:892–916`, `github-git-provider.ts:469–478`). API raw-secret guard at `apps/api/src/main.ts:541–548` rejects writes carrying token-like values. Dashboard sanitizer at `shared/dashboard-read-models.ts:334–335`. Tested in `tests/real-git-adapter-v1.test.ts:269–280` and `tests/real-git-adapter-v2.test.ts:487–545`. |
| **Branch prefix / allowlist enforced** | pass | `allowedBranchPrefix = "ai/"` enforced in `validateWrite` and `validatePullRequest` (`github-git-provider.ts:388, 405`). `repoAllowed(slug)` enforces `AICHESTRA_GITHUB_ALLOWED_REPOS` at read & write time (`github-git-provider.ts:373, 385, 402, 418–420`). Webhook receiver enforces `webhookAllowedRepos` (`webhooks.ts:191–203`). Worker-generated branch names always start with `ai/${task.id}/...` (`workflow.ts:170`). |
| **Policy denial wins** | pass | In every remote write path, policy is evaluated **before** the provider call (`service.ts:400–447`). If policy denies, the operation is short-circuited with `policy_denied` and audited, regardless of whether the gate config would otherwise have allowed it. Webhook receiver evaluates three separate actions (`verify`, `receive`, `process`) and rejects on any denial (`webhooks.ts:225–292`). The static policy engine is **deny-by-default**: when no rule matches, the decision is `deny` with `policy_default_deny` (`policy/src/engine.ts:99–112`). |

---

## 5. Validation

| Command | Result | Detail |
|---|---|---|
| `pnpm lint` | **pass** | `scripts/lint.mjs` walked the tree — no trailing whitespace, no `fetch("http..."` / `https.request(` matches, all JSON parses. |
| `pnpm typecheck` | **pass** | `tsc --noEmit -p tsconfig.typecheck.json` clean. |
| `pnpm test` | **pass** (3 consecutive runs) | 228 tests / **224 pass / 0 fail / 4 skipped** (the 4 skips are optional Postgres + GitHub/LLM integration tests requiring explicit env gates). The flake observed in the previous audit (`local-agent-protocol-v1.test.ts:288`) did **not** reproduce in any of three back-to-back runs today. |
| `pnpm build` | **pass** | Doc/migration/source presence checks + `node --check` on `apps/api/src/main.ts`, `apps/worker/src/main.ts`, `apps/web/src/main.ts`. |
| `git diff --check` | **pass** | Clean working tree, no whitespace errors. |

---

## 6. Final Status

**Phase 1 status**: `complete_for_current_milestone` — every requested surface (task model, taskRun model, create API, run API, worker path, policy check, registry selection, branch, agent run, diff, test result, PR, usage ledger, attribution, dashboard/API visibility, tests) is implemented and verified by passing tests. Mock-first defaults intact.

**Phase 2 status**: `complete_for_current_milestone` — `BranchLease`, `ConflictRisk`, `MergeQueueEntry`, `MergeSimulationResult`, file-overlap scoring, `LocalGitDryRunMergeSimulator`, all four simulation statuses, merge-queue recommendation, changed-files linkage, PR/branch sync read models, and no-merge/no-rebase/no-force-push/no-branch-delete constraints are all in place and tested. Local dry-run uses `git merge-tree --write-tree` only against caller-supplied paths through the `AICHESTRA_ALLOWED_REPO_PATHS` allowlist, per AGENTS.md rule 53.

**Real Git Adapter status**: `v2_implemented` (gated). MockGitProvider default; LocalGitProvider local-only; GitHubGitProvider gated; GitHubClient is the single real-fetch boundary; branch/PR/webhook gates re-checked on every call; HMAC-SHA256 verifier with constant-time comparison; SecretRef-first credential resolution with audited legacy env fallback; full redaction at config/audit/dashboard layers; default tests make zero GitHub calls.

**Critical blockers**: **none**.

**Important follow-ups**:
1. **Runtime webhook dedupe & replay rejection** (`packages/git-adapter/src/webhooks.ts` + `packages/db/src/repository.ts:398–406`). Currently the planning surface in `packages/deployment-readiness` defines `replayStatus`, `dead_letter`, and metrics, but the runtime receiver re-records duplicate `deliveryId`s. Before enabling production webhook traffic, implement: idempotent insertion keyed by `deliveryId`, "same deliveryId different payloadHash" rejection, dead-letter status for `failed`, and an audit event for `github_webhook_duplicate_rejected`. Tracked in `docs/roadmaps/github-app-production-webhook-hardening/`.
2. **Negative-path test for SecretRef vs. legacy env fallback** in `tests/real-git-adapter-v1.test.ts` and `tests/real-git-adapter-v2.test.ts` — assert that `disabled` / `revoked` `_SECRET_REF` does **not** silently fall back to a configured legacy `AICHESTRA_GITHUB_TOKEN` / `AICHESTRA_GITHUB_WEBHOOK_SECRET`. The current behaviour is correct in `provider-factory.ts:169–187` and `github-webhooks.ts:151–186`, but lacks a direct assertion.
3. **Test-isolation investigation for `tests/local-agent-protocol-v1.test.ts:288`** — flagged as a flake by the prior current-state audit; did not reproduce in three consecutive runs today, but the test exercises shared mock-state (capability advertisements + invocation envelopes) and may interact with prior tests. Worth confirming with a `node --test --concurrency=1` and `--shuffle` run before declaring it stable.
4. **Consolidate policy engines**: the worker workflow still uses the legacy `MockPolicyEngine` (`@aichestra/policy`'s `LegacyTaskPolicyEngine`) while the API uses the modern `StaticPolicyEngine` everywhere else. Aligning the worker on `PolicyService` removes a parallel codepath and unifies the deny-by-default semantics.
5. **Tighten lint regex** (`scripts/lint.mjs:37–39`) to also catch `fetch(\`https://...\`)` and `fetch(new URL("https://..."))` template-literal/URL forms as defense-in-depth.

**Recommended next task**:
**Implement webhook delivery dedupe + replay-rejection at the runtime layer** (Follow-up #1), since it is the only Phase-2 / Real Git Adapter v2 surface where the planning surface is ahead of the runtime — and it directly gates the next production-hardening track (GitHub App webhook v1). Add (a) `recordGitWebhookEvent` idempotency by `deliveryId`, (b) `same-deliveryId-different-payloadHash` → `replay_rejected` + audit, (c) a dead-letter status for processing failures, and (d) two tests (duplicate first-seen returns 202 ignored; conflicting payload hash returns 409 replay_rejected). This unblocks moving the GitHub App / Production Webhook Hardening track from `planned_only` to `v1_implemented` without touching any other phase.
