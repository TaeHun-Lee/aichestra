# Aichestra — Integration Foundations Audit

> **Audit date**: 2026-05-14
> **Reviewer**: Claude (claude-opus-4-7, 1M context)
> **Working tree**: clean at `main` @ `61a4036 feat: Secret Backend Migration Planning v0`
> **Scope**: audit-only review of the integration-foundation tracks — Persistent DB, Real Git Adapter, LLM Gateway, MCP Gateway, Runner / Local Agent, Auth/Policy/Secrets, Observability, Dashboard.

---

## 1. Persistent DB

| # | Surface | Status | Evidence | Concerns |
|---|---|---|---|---|
| 1.1 | **Storage provider abstraction** | implemented | `packages/db/src/storage.ts:17–92` — `StorageProviderKind = "in_memory" | "persistent" | "postgres"`, `StorageProvider` interface (`kind`, `repositoryFactory`, `createUnitOfWork?`, `healthCheck`, `close?`), with explicit `TaskRepository`, `TaskRunRepository`, `UsageLedgerRepository`, `ConflictRepositories`, `RegistryServiceInput`, `ImprovementRepository` boundaries on `RepositoryFactory:75–84`. | None. |
| 1.2 | **Repository factory** | implemented | `InMemoryRepositoryFactory` (`storage.ts:100–155`) returns the in-memory `InMemoryAichestraStore` for every interface; `PostgresRepositoryFactory` (`packages/db/src/postgres.ts`) provides Postgres-backed implementations for the same interfaces. `apps/api/src/main.ts:4154–4158` consumes only the factory contract (`storage.repositoryFactory.createRegistryRepositories()`, etc.), so API handlers never bind to a specific provider. | None. |
| 1.3 | **Postgres opt-in** | implemented | `createApiStorageProviderFromEnv` (`apps/api/src/main.ts:4299–4304`) routes `AICHESTRA_STORAGE_PROVIDER=postgres` to `createPostgresStorageProviderFromEnv(env)`; otherwise returns in-memory. `createPostgresStorageProviderFromEnv` (`packages/db/src/postgres.ts:933–942`) throws **before** connecting if `AICHESTRA_DATABASE_URL` is missing. Default runtime never instantiates the Postgres client. | None. |
| 1.4 | **Migration runner** | implemented (operator-invoked only) | `scripts/db/migrate.mjs` runs only when `AICHESTRA_DATABASE_URL`/`DATABASE_URL` is set; otherwise exits with error. Uses `psql -X -v ON_ERROR_STOP=1 ... -f <migration>`. Each `.sql` file under `infra/migrations/` is applied in sorted order. **Migrations are never run by `pnpm install`, `pnpm test`, `pnpm build`, or app startup** — `scripts/build.mjs` only verifies file presence; `apps/api/src/main.ts:4314–4321` calls `healthCheck()` but never executes migrations. | None. |
| 1.5 | **Schema skeleton** | implemented | `infra/migrations/0001_initial_aichestra_schema.sql` (1,264 lines) declares 25+ tables covering Task, TaskRun, UsageEvent, BranchLease, MergeSimulationResult, MergeQueueEntry, Skill/Harness/Instruction registries, registry audit/history/eval/packages, Phase 4 improvement artifacts, etc. `packages/db/prisma/schema.prisma` exists for design reference (required-path-asserted by `scripts/build.mjs`). | None for v1 scope. |
| 1.6 | **Repository contract tests** | implemented | `tests/repository-contracts.test.ts:9–206` defines a single `runRepositoryContracts(name, createProvider)` matrix covering tasks, taskRuns, usage, branch leases + conflict risks, merge simulation, merge queue, registry create/audit/history/manifest, improvement signals/clustering/candidates/proposals/draft changes/decisions/eval requirements/eval runs. Same suite is reused for the in-memory and the Postgres providers. | None. |
| 1.7 | **Default tests do not require DB** | implemented | The in-memory contract test (`repository-contracts.test.ts:208–210`) runs without env. `pnpm test` is otherwise wired against `createSeededStore()` (in-memory) — confirmed by surveying `tests/*.test.ts` imports. No test attempts a real `psql` call by default. | None. |
| 1.8 | **Optional Postgres tests skip correctly** | implemented | `repository-contracts.test.ts:212–224` uses `node --test`'s `skip` option keyed on `process.env.AICHESTRA_TEST_DATABASE_URL`. Validation run shows `﹣ postgres storage provider satisfies repository contracts when a test database URL is configured (0.171ms) # Set AICHESTRA_TEST_DATABASE_URL to run optional Postgres repository contract tests.` — confirmed skipped. | None. |

**Persistent DB verdict**: `v1_implemented` (opt-in Postgres) + `v1_implemented` for Persistent DB Production Operations Planning (read-only planning surface only, per AGENTS.md rules 71–73).

---

## 2. Real Git Adapter

| # | Surface | Status | Evidence | Concerns |
|---|---|---|---|---|
| 2.1 | **v2 status** | `v2_implemented` (gated) | All three providers live: `MockGitProvider` (default), `LocalGitProvider` (`AICHESTRA_GIT_PROVIDER=local`), `GitHubGitProvider` (`AICHESTRA_GIT_PROVIDER=github`). Provider factory: `packages/adapters/src/git/provider-factory.ts:95–128`. Service: `packages/git-adapter/src/service.ts` (928 LOC). Webhook receiver+sync: `packages/git-adapter/src/webhooks.ts` (937 LOC). | None. |
| 2.2 | **GitHub gated branch/PR** | implemented | `GitHubGitProvider.validateWrite` (`github-git-provider.ts:377–392`) chains `remoteGitEnabled` → `remoteBranchCreateEnabled` → `configured (token)` → `resolveTarget` → `repoAllowed(slug)` → `branchName.startsWith(allowedBranchPrefix)`. Equivalent `validatePullRequest` (`394–409`) for PR creation with `remotePullRequestCreateEnabled`. `FetchGitHubClient` is only instantiated when `remoteGitEnabled && token && allowedRepos.length > 0` (`provider-factory.ts:111–113`); otherwise `NoopGitHubClient` returns blocked from every call. | None. |
| 2.3 | **Webhook receiver / verifier** | implemented | `packages/adapters/src/git/github-webhooks.ts:189–273` provides `NoopGitHubWebhookVerifier` (default), `MockGitHubWebhookVerifier`, and `HmacGitHubWebhookVerifier` (sha256 HMAC + `timingSafeEqual` constant-time compare on line 263–272). `createGitHubWebhookRuntimeFromEnv` (`github-webhooks.ts:122–141`) constructs the HMAC verifier only when `webhooksEnabled && secret.ok && secret.value`. Receiver `git-adapter/src/webhooks.ts:115–325` enforces: webhooks-enabled → secret configured → JSON-parsable body → required headers → repo allowlist → signature verified → policy `git.webhook.verify/receive/process` allowed → known event. | None. |
| 2.4 | **PR/branch sync read model** | implemented | `GitPullRequestSyncState` and `GitBranchSyncState` upserted by `GitHubSyncService` from verified `pull_request` and `push` payloads; never destructive. Manual sync (`POST .../pull-requests/:n/sync`) and `refresh-changed-files` re-validate gates. API: `/git/repos/:repoId/{pr-sync,branch-sync}` (`apps/api/src/main.ts:3590–3617`). | None. |
| 2.5 | **No destructive ops** | implemented (structurally unsupported) | No code path implements merge, rebase, force-push, or branch-delete on any provider. `remoteMergeEnabled = false` is hard-coded everywhere (`provider-factory.ts:75`, `github-git-provider.ts:131`, `service.ts:100,140,700,834`, `interfaces.ts:166`, `shared/dashboard-read-models.ts:24`). `AICHESTRA_ALLOW_REMOTE_MERGE` is never read in code. Push webhook only updates metadata. | None. |
| 2.6 | **Policy / Auth / SecretRef / audit integration** | implemented | Policy: `evaluateRemotePolicy` called before every remote write (`service.ts:400–448`); webhook receiver evaluates `git.webhook.verify`, `git.webhook.receive`, `git.webhook.process` separately (`webhooks.ts:225–292`). SecretRef: provider factory resolves the GitHub token via `options.credentialResolver` first (`provider-factory.ts:164–209`); webhook secret via `secretResolver` (`github-webhooks.ts:143–187`); legacy env is audited through `legacyCredentialFallbackAuditor`/`legacySecretFallbackAuditor` wired into `securityService.recordSecretAudit` (`apps/api/src/main.ts:4159–4210`). Audit/redaction: `sanitizeMetadata` strips token/secret/credential/authorization/api-key keys and pattern-redacts `ghp_*`, `github_pat_*`, `Bearer ...` (`service.ts:892–916`). | Same minor follow-up carried over: no direct negative-path test asserting that a `disabled`/`revoked` `_SECRET_REF` does not fall back to a configured legacy env. Behaviour is correct (provider-factory.ts:169–187, github-webhooks.ts:151–186) but not asserted by a dedicated test. **Production-readiness item**: runtime webhook delivery dedupe / replay-rejection is **planning-only** in `packages/deployment-readiness/src/{types,catalog,service}.ts`; the runtime receiver re-records duplicate `deliveryId`s. Tracked under `docs/roadmaps/github-app-production-webhook-hardening/`. |

**Real Git Adapter verdict**: `v2_implemented` (gated; safe by default).

---

## 3. LLM Gateway

| # | Surface | Status | Evidence | Concerns |
|---|---|---|---|---|
| 3.1 | **v2 status** | `v2_implemented` (gated) | `packages/llm-gateway/src/{providers,gateway,routing,model-router,virtual-keys,catalog,enterprise-providers,local-agent-protocol,types,dto}.ts`. Total ~8.2k LOC. Default service via `createDefaultLlmGatewayService` (`apps/api/src/main.ts:4218–4224`). | None. |
| 3.2 | **Multi-provider routing** | implemented | `LLMRouteRepository` + `seedLlmRoutes` (`routing.ts:143–263`) includes one mock-general/coder/conflict/registry route, one gated OpenAI-compatible route (`route_openai_compatible_default`, requires remote + SecretRef), six disabled skeleton routes (Anthropic/Gemini/Bedrock/Vertex/Azure/LiteLLM, all `metadata: { skeleton: true, implemented: false, noNetworkCalls: true }`), and one Local CLI bridge (`directInvocation: false`, `localAgentRequired: true`). | None. |
| 3.3 | **Fallback safety** | implemented | `createDefaultFallbackPolicy` (`routing.ts:122–141`) is `enabled: false`, `maxAttempts: 0`, `allowedProviderKinds: ["mock"]`, `disallowedProviderKinds: ["local_cli"]`, `requireSameDataClass`, `requirePolicyAllow`, `requireBudgetRemaining`, `stopOnPolicyDeny`, `stopOnCredentialDeny`, `stopOnBudgetDeny` all true. `LLMGatewayService.tryFallback` (`gateway.ts:1168–1235`) short-circuits when `!fallbackEnabled || maxAttempts === 0 || !route.fallbackAllowed`, evaluates `llm.fallback` policy before any fallback attempt, records the routing decision as `policy_blocked` on denial, and excludes the original route. | None. |
| 3.4 | **Model catalog** | implemented | `seedLlmModels()` in `providers.ts:472+` plus skeleton metadata. Model registry queries route via `getModel` and the catalog respects `allowedModels` (env `AICHESTRA_LLM_ALLOWED_MODELS`) and `defaultModel`. Filter logic enforces `allowedProviderKinds`, `allowedProviderIds`, `deniedProviderIds`, `deniedModels` from `LLMProviderRuntimeConfig`. | None. |
| 3.5 | **Provider skeletons** | implemented | `DisabledSkeletonLLMProvider` (`providers.ts:308–347`) with subclasses for `AnthropicCompatible`, `GeminiCompatible`, `BedrockCompatible`, `VertexCompatible`, `AzureCompatible`, `LiteLLMCompatible`, and `LocalCliLLMProviderBridgeSkeleton` (line 385–389; `localAgentRequired: true`). All return `blocked` with `reason: "<kind>_provider_not_implemented"` from `createCompletion` and `validateConnection`. | None. |
| 3.6 | **OpenAI-compatible gated real path** | implemented | `OpenAICompatibleLLMProvider` (`providers.ts:161–222`). `createCompletion` returns `{ ok: false, reason: "blocked_remote_llm_disabled" }` unless `remoteLlmEnabled`, then `"remote_llm_completion_disabled"` unless `remoteCompletionEnabled`, then `"openai_compatible_config_missing"` unless `baseUrl && apiKey`. Real `fetch` is in `FetchOpenAICompatibleHttpClient.postJson` (`providers.ts:134–158`) with `AbortController` + configurable `timeoutMs`. | None. |
| 3.7 | **SecretRef / Auth / Policy / Budget / Usage / Audit integration** | implemented | Credential resolution flows through `securityService.resolveCredentialForInternalUse` (wired at `apps/api/src/main.ts:4222`); legacy env fallback audited via `legacyCredentialFallbackAuditor` → `securityService.recordSecretAudit`. Policy: `evaluateCompletionPolicy` and `evaluateFallbackPolicy` called inside the gateway (`gateway.ts:434+, 1180+`). Auth: `request.authContext` and `request.actorId` thread through into the policy subject. Budget: `VirtualModelKeyRepository.createDefaultVirtualModelKey()` (`virtual-keys.ts:83–100`) defaults `allowedProviderKinds: ["mock"]`, `monthlyBudgetUsd: 100`, `perTaskBudgetUsd: 5`, `rpmLimit: 120`, `tpmLimit: 120000`. `budgetDecision` is required to be `allowed` before any provider is invoked (`gateway.ts:392`). Usage: `usageRepository.recordUsage` records `taskId`/`taskRunId` on every successful completion. Audit: `recordAuditEvent` records `llm_routing_decision`, `llm_completion`, `llm_fallback_attempt_blocked`, etc. | None. |
| 3.8 | **No real calls by default** | implemented | Default env yields `providerKind: "mock"`, `routingMode: "mock_only"`, `fallbackEnabled: false`, `maxFallbackAttempts: 0`, `remoteLlmEnabled: false`, `remoteCompletionEnabled: false`. The only real-`fetch` site is `providers.ts:139`, gated upstream. `scripts/lint.mjs` enforces a ban on hard-coded `fetch("http..."` in the rest of the tree. Optional integration tests skip unless `AICHESTRA_LLM_INTEGRATION_TESTS=true` is set. | None. |

**LLM Gateway verdict**: `v2_implemented` (gated; mock-only by default; only OpenAI-compatible has a real path, all other provider kinds are disabled skeletons).

---

## 4. MCP Gateway

| # | Surface | Status | Evidence | Concerns |
|---|---|---|---|---|
| 4.1 | **v0 status** | `v0_implemented` | `packages/mcp-gateway/src/{gateway,catalog,repository,types,dto}.ts`. Constructed at `createDefaultMCPGateway({ policyService, authorizationService, securityService })` (`apps/api/src/main.ts:4225–4229`). | None. |
| 4.2 | **Mock-only gateway** | implemented | `MockMCPGateway.getGatewayKind()` returns `"mock"` (`gateway.ts:102–104`). `MCPGatewayConfig.realTransportEnabled = false`, `externalCallsEnabled = false`, `secretForwardingEnabled = false`, `networkAccessEnabled = false` (`gateway.ts:109–119`). Real MCP transport is *not implemented at all*; `validateInvocation` returns `unavailable` for any non-mock `serverKind` (`gateway.ts:217–219`). | None. |
| 4.3 | **Server / tool catalog** | implemented | `packages/mcp-gateway/src/catalog.ts` (271 LOC) seeds a deterministic set of mock servers/tools with explicit `riskLevel`, `requiredSecretRefs`, `requiredScopes`, `metadata.writeOperation`, `.deployOperation`, `.networkRequired`, `.localExecutionRequired`. Repositories: `InMemoryMCPServerRepository`, `InMemoryMCPToolRepository` (`repository.ts`). | None. |
| 4.4 | **Risk levels** | implemented | `MCPRiskLevel` enum (`types.ts`); `riskAction()` (`gateway.ts:29–33`) maps `low → mcp.tool.invoke.low_risk`, `medium|high → mcp.tool.invoke.high_risk`, `critical → mcp.tool.invoke.critical`. Each invocation runs through the risk-specific policy action **and** the generic `mcp.tool.invoke` action. | None. |
| 4.5 | **Auth / Policy / Secret / Sandbox integration** | implemented | Auth: every list/invoke gets an `AuthContext` (constructed via `authorizationService.getAuthContext` if not supplied) and an `authorize(...)` decision (`gateway.ts:122–187`). Policy: risk-specific `+` generic `mcp.tool.invoke` policy actions evaluated (`gateway.ts:226–268`). Secret: tools / servers with `requiredSecretRefs.length > 0` are short-circuited to `secret_denied` with `mcp_tool_secret_resolution_disabled_v0` (`gateway.ts:282–293`) — **never resolves real secrets**. Sandbox / network: `tool.metadata.networkRequired === true` triggers `securityService.evaluateNetworkEgress` and returns `mcp_network_access_not_implemented_v0` (`gateway.ts:295–312`). | None. |
| 4.6 | **No auto tool execution** | implemented | The gateway exposes `validateInvocation` and `invokeTool` only — there is no LLM-side automatic dispatch. `tool.metadata.writeOperation` is hard-denied with `mcp_write_tool_denied_v0`, `deployOperation` denied with `mcp_deploy_tool_denied_v0`, `localExecutionRequired` returns `unavailable` with `local_agent_transport_required_not_implemented_v0` (`gateway.ts:314–351`). | None. |
| 4.7 | **No real MCP transport** | implemented | No stdio/http/sse MCP client exists in the package. The `serverKind !== "mock"` branch returns `unavailable` with `real_mcp_transport_disabled` (`gateway.ts:217–219`). | None. |
| 4.8 | **No output/input secret leakage** | implemented | Both input (`request.input`) and output payloads pass through `sanitizeMCPValue` / `sanitizeMCPString` (`gateway.ts:51–77`) which strips `*_API_KEY/TOKEN/SECRET/PASSWORD = ...`, `Bearer ...`, `sk-*`, `ghp_*`, `github_pat_*`, `~/.codex/auth.json`, `~/.claude*`, `Google credential cache`. `sanitizeSecurityMetadata` is applied first. Audit records use `sanitizedMetadata` field exclusively. | None. |

**MCP Gateway verdict**: `v0_implemented` (mock-only, no real transport).

---

## 5. Runner and Local Agent

| # | Surface | Status | Evidence | Concerns |
|---|---|---|---|---|
| 5.1 | **Local Agent Runner v1** | implemented | `packages/runner/src/local-agent-runner.ts` (489 LOC). `LocalAgentRunner.constructor` defaults `enabled = false`, `allowCommandExecution = false` (`:97–105`). If `allowCommandExecution` is true, `FixtureLocalCommandExecutor` is wired; otherwise `BlockedCommandExecutor` (`:106–112`). `apps/api/src/main.ts:4231–4242` constructs `MockAgentRunner` for `runnerKind === "mock"` and only otherwise constructs the local runner. | None. |
| 5.2 | **Command execution disabled by default** | implemented | `createAgentRunnerConfigFromEnv` (`runner/src/config.ts:25–40`) defaults `localCommandExecutionEnabled` to `false`; sets `commandExecutorKind = "blocked"` unless `AICHESTRA_ALLOW_LOCAL_COMMAND_EXECUTION=true`. `BlockedCommandExecutor.validateCommand` returns `{ allowed: false, reason: "command_executor_blocked", blockedReason: "local_command_execution_disabled" }` (`command-executor.ts:140–147`). | None. |
| 5.3 | **Fixture command boundary** | implemented | `FixtureLocalCommandExecutor` (`command-executor.ts:161–343`): rejects shell metacharacters (`[|;&<>\`$]`) in command or args, hard-deny list `curl/wget/git/kubectl/vault/temporal/mcp/rm/rmdir/del/powershell/pwsh/cmd/bash/sh` and `.exe`/`.cmd` variants, restricts to `allowedFixtureExecutables = {node, node.exe, pnpm, pnpm.cmd}` (or `process.execPath`), then enforces harness allow/deny lists. Spawn options: `shell: false`, `windowsHide: true`, `stdio: ["ignore","pipe","pipe"]`, no env inheritance (`envForRequest` strips any key matching `/token|secret|key/i`). Hard byte caps (`maxStdoutBytes`/`maxStderrBytes` default 4096) and timeout (`request.timeoutMs`) enforced. Output passed through `sanitizePreview` (`command-executor.ts:76–80`) which strips `*_TOKEN/SECRET/KEY=...` to `[redacted]` before audit. | None. |
| 5.4 | **Workspace manager** | implemented | `LocalAgentWorkspaceManager` (`workspace.ts`). `validateWorkspace` (`:79–92`) rejects: workspace outside `workspaceRoot`, the repository root unless explicitly allowed via fixture flag, the filesystem root. `createTempWorkspacePath` creates a `mkdtemp` under the configured root (defaults to `os.tmpdir()/aichestra-agent-workspaces`). `cleanupWorkspace` only runs `rm` when `cleanupPolicy === "delete_temp_workspace"` and `workspace.mode === "temp"`. | None. |
| 5.5 | **Harness policy** | implemented | `createRunnerHarnessPolicy` (`harness-policy.ts:41–68`) sets `allowGitRemote: false`, `allowFileWrite: false` (default), `allowSecrets: false` (default), `allowNetwork: false` unless harness explicitly `networkPolicy.mode === "unrestricted"`. Default deny list at `:24–39` blocks `git fetch/push/merge/rebase`, `rm -rf`, `rmdir`, `del`, `remove-item`, `curl`, `wget`, `kubectl`, `vault`, `temporal`, `mcp`. `evaluateRunnerHarnessPolicy` (`:70–108`) hard-rejects denied commands, network commands (`/\b(curl|wget|fetch)\b/i`), and remote git commands (`/\bgit\s+(fetch|push|merge|rebase)\b/i`) — *regardless of harness configuration*. | None. |
| 5.6 | **No shell / remote git / secrets by default** | implemented | See 5.3 and 5.5. The command executor reads `process.env` only for allow-listed keys and **filters out any key matching `/token|secret|key/i`** (`command-executor.ts:88–100`). Harness allow-list mode (`allowInheritedEnv = false` by default) keeps the env minimal. | None. |
| 5.7 | **Local Agent Protocol v1** | implemented (mock-only) | `packages/llm-gateway/src/local-agent-protocol.ts` (~2,815 LOC). `LocalAgentProtocolService` orchestrates registration, channels, capability advertisement, compatibility checks, consent envelopes, invocation envelopes, dispatch, and event streams. Wired into `ProviderAbstractionService` for `local_cli` providers (`enterprise-providers.ts:870–1000`). | One **test-isolation flake** observed: `tests/local-agent-protocol-v1.test.ts:288` (Enterprise Provider local_cli compatibility ordering) — passes in **isolation** (verified: `node --test tests/local-agent-protocol-v1.test.ts` runs 5/5 green), but **fails intermittently inside the full `pnpm test` run** with `actual: 'awaiting_consent'` vs `expected: 'provider_template_incompatible'`. The flake suggests cross-test state leakage in a singleton repository (likely capability advertisements or invocation envelopes) when test files share process state. **No safety gate is relaxed** — direct CLI execution remains blocked either way. |
| 5.8 | **Fixture daemon** | implemented | `LocalAgentProtocolService.startFixtureAgent` (`local-agent-protocol.ts:1514+`) registers an agent with `status: "connected"`, `metadata.fixtureDaemon = true`, `noNetworkTransport: true`, `vendorCliExecution: false`, then auto-advertises a default capability set. The fixture daemon never executes real binaries — completion responses are synthesized. | None. |
| 5.9 | **Mock signed channel** | implemented | `MockLocalAgentTransport` is the only transport; `connectChannel`/`disconnectChannel` records `LocalAgentChannel { channelKind: "mock_in_memory" }` and a fake handshake. Real channel kinds (`future_websocket | future_grpc | future_http_tunnel`) are denied by the static policy engine for `local_agent.channel.create` (default rule). | None. |
| 5.10 | **Consent / streaming / capability / compatibility** | implemented | Consent: `createInvocationEnvelope` creates `requiredConsentLevel` + consent request; `recordConsentDecision` flips the invocation to `consent_approved/denied`. Streaming: `receiveInvocationEvent` normalizes redacted stream events (no raw payloads). Capability: `advertiseCapabilities` (`:1369–1420`) tracks `supportedProviderTemplates`, `supportedParserModes`, `supportedConsentLevels`, `supportedSandboxKinds`, `supportsStreaming`, `supportsCancellation`. Compatibility: `checkCompatibility` (`:1434–1508`) compares the requested template/parser/version against the advertised set and the seed matrix; emits `provider_template_incompatible`, `provider_template_not_advertised`, `parser_mode_not_advertised`, or `fixture_cli_version_incompatible`. | The 5.7 flake suggests the order of compatibility-vs-consent gates can interact with prior test state. |
| 5.11 | **No real vendor CLI** | implemented | No `spawn("codex" \| "claude" \| "gemini" \| ...)`. `LocalCliLLMProviderBridgeSkeleton` (`providers.ts:385–389`) blocks the path with `reason: "local_agent_required", localAgentRequired: true`. `ProviderAbstractionService.invoke` for `local_cli` requires a connected channel + compatibility + consent + dispatch through the mock protocol; the fixture daemon synthesizes the result without executing any binary. Direct local CLI execution from Cloud is **explicitly denied** by static policy (`packages/policy/src/default-rules.ts` rule for `local_cli.execution.cloud_initiated`). | None. |

**Runner / Local Agent verdict**: `v1_implemented` (mock-first; one v1-protocol test isolation flake to investigate).

---

## 6. Auth / Policy / Secrets

| # | Surface | Status | Evidence | Concerns |
|---|---|---|---|---|
| 6.1 | **Auth/RBAC v0** | `v0_implemented` (planning + MockAuthProvider) | `packages/auth/src/{providers,service,repository,types,catalog,request-context,dto}.ts`. `MockAuthProvider.resolveAuthContext` records a `mock_actor_used` audit event with `reason: "MockAuthProvider is default and is not production auth."` (`providers.ts:130–138`). All future providers (`FutureOidcAuthProviderPlaceholder`, `FutureSamlAuthProviderPlaceholder`, `FutureScimDirectoryPlaceholder`, `FutureServiceAccountAuthProviderPlaceholder`, `providers.ts:170–226`) throw `${kind} is disabled and not implemented in Production Auth/RBAC Planning v0.` from `resolveAuthContext`. `AuthorizationService.toPolicySubject` (`service.ts:94–110`) is the single feeder into `PolicySubject`. | None for v0; production Auth/RBAC v1 is the next planning track. |
| 6.2 | **Policy-as-code v0** | `v0_implemented` | `packages/policy/src/{engine,default-rules,types,audit,dto}.ts`. `StaticPolicyEngine.evaluate` (`engine.ts:90–127`) is **deny-by-default**: no matching rule ⇒ `{ allowed: false, decision: "deny", matchedRuleIds: ["policy_default_deny"], reason: \`No policy rule allowed ${action} on ${resourceKind}.\` }`. Default rules (`default-rules.ts:652+`) deny remote-git operations without gates, deny unverified webhook processing, deny remote LLM completions without gates, deny secret reads, deny network egress, deny runner secret injection, deny MCP tool calls, deny Local Agent secret forwarding, deny improvement apply. | None. |
| 6.3 | **Secrets/Sandbox v0** | `v0_implemented` (metadata-only) | `packages/security/src/{types,credentials,service,redaction,repository,dto}.ts`. `SecretRef` has **no `value` field** (`types.ts`); `SandboxProfile` includes `container`, `firecracker`, `kubernetes` as **future placeholders only**; `NetworkEgressPolicy` defaults to deny. `MockSecretManager` never returns raw secret material; `EnvSecretProvider` defaults disabled (`credentials.ts:21–63`). | None. |
| 6.4 | **SecretRef v1** | `v1_implemented` | `CredentialManager.resolveCredentialForInternalUse` (`security/src/service.ts:641–952`) gates through Authorization → purpose policy → credential policy → `secret.lease.request` → `secret.lease.issue` before the env value is read. Public DTO path strips `value` (`service.ts:236–240`). API raw-secret detector (`apps/api/src/main.ts:541–548`) rejects writes with `value/secretValue/rawSecret/token/apiKey/credentialValue/password` fields or token-like strings. SecretRef wired into Git provider factory and webhook, and into the OpenAI-compatible LLM provider. | Same v1-completion-review follow-up: add explicit negative test for `disabled`/`revoked` SecretRef vs. legacy env fallback. |
| 6.5 | **Deny-by-default** | implemented | See 6.2. Verified in the policy engine and the static rule catalog. Tests: `tests/policy-as-code-v0.test.ts` exercises this. Default `MockPolicyEngine` (legacy adapter) also defaults to allow only for explicit MVP actions; the `StaticPolicyEngine` is what's wired in the API/MCP/LLM/Git paths. | None. |
| 6.6 | **No production auth claims** | implemented | `MockAuthProvider` returns `"mock_auth_context_resolved"` reason on every successful resolve; audit event flagged with `productionAuthEnabled: false`. `/auth/config` exposes the auth mode as `"mock"`. README "Next Steps" §1 explicitly lists Production Auth/RBAC v1 planning as the next track. No login/logout/password/token/session endpoint exists. | None. |
| 6.7 | **No raw secret exposure** | implemented | Defense-in-depth at three layers: (a) `apps/api/src/main.ts:541–548` `containsRawSecretField` rejects raw-secret request bodies; (b) `packages/security/src/redaction.ts:17–46` pattern-redacts `*_API_KEY`, `*_TOKEN`, `Bearer ...`, `sk-*`, `ghp_*`, `github_pat_*`, `~/.codex/auth.json`, `~/.claude*`, `application_default_credentials.json`; (c) `packages/observability/src/sanitizer.ts:17–18,77` and `packages/shared/src/dashboard-read-models.ts:334–335` apply the same patterns at audit/dashboard layers. `/health`, `/security/credentials/*`, `/git/config`, `/llm/config`, `/dashboard/*` all expose only `status/source/reason` booleans/strings — never the value. | None. |

**Auth / Policy / Secrets verdict**: all three are `v0_implemented` plus SecretRef-backed Provider Credentials `v1_implemented`.

---

## 7. Observability / Audit Retention

| # | Surface | Status | Evidence | Concerns |
|---|---|---|---|---|
| 7.1 | **Common audit envelope** | implemented | `AuditEventEnvelope` in `packages/observability/src/types.ts` with `id`, `category`, `outcome`, `severity`, `actorId`, `principalId`, `taskId`, `taskRunId`, `repoId`, `resourceKind`, `resourceId`, `action`, `summary`, `metadata`, `retentionClass`, `redactionClass`, `occurredAt`, `sourceModule`. Constructed by `observabilityService.normalizeEvent(...)` from each domain audit list (`apps/api/src/main.ts:4254–4272` wires `coreAuditLogs`, `authAuditEvents`, `policyAuditEntries`, `securityAuditEvents`, `gitAuditEvents`, `gitWebhookAuditEvents`, `llmAuditEvents`, `mcpAuditEvents`, `agentRunAuditEvents`, `registryAuditLogs`, `improvementGovernanceAuditEvents`, `localAgentAuditEvents`, `providerAuditEvents`, `deploymentReadinessChecks`, `deploymentRisks`). | None. |
| 7.2 | **Retention / redaction classes** | implemented | `defaultAuditRetentionClasses`, `defaultAuditRedactionClasses`, `defaultAuditRetentionPolicies` in `packages/observability/src/catalog.ts:1+`. Classes include `operational`, `security`, `compliance`, `forensic`, etc. with explicit `retentionDays` and `description` (read-only metadata — no deletion enforced). | None. |
| 7.3 | **Audit source normalization** | implemented | `sourceDefinitions` array in `service.ts:78+` declares 14 source modules (core, auth, policy, credential, git, git_webhook, llm, mcp, runner, registry, improvement, local_agent, security, system) with `auditSource`, `currentRepositoryStorage`, `normalized: "yes"/"partial"/"no"`, `sourceModule`, `category`, `retentionClass`, `redactionClass`, `missingGaps`. `AuditSourceCoverage` DTO surfaces this on `/observability/audit/sources` and `/dashboard/observability`. | None. |
| 7.4 | **Metrics / trace skeleton** | implemented | `defaultMetricDefinitions` in `catalog.ts`; `metricSnapshot()` (`service.ts:600+`) emits read-only metric points such as `observability.external_exporter.enabled = 0` (with label `exporter: "none"`) — confirming no real exporter is connected (`service.ts:669`). Trace skeleton: `TraceSpan` type with `traceId`, `spanId`, `parentSpanId`, `status`, no real OTel/Jaeger backend. | None. |
| 7.5 | **Observability API / Dashboard** | implemented | `/observability/config`, `/observability/audit/{events,summary,retention-classes,redaction-classes,sources}`, `/observability/metrics`, `/observability/metrics/snapshot`, `/observability/traces` exposed in `apps/api/src/main.ts`. Dashboard panel renders via `/dashboard/observability`. Read-only — no mutation endpoints. | None. |
| 7.6 | **No external exporter** | implemented | No OpenTelemetry / Jaeger / Prometheus / SIEM exporter library is imported. The only `external_exporter` reference is the read-model metric point that confirms exporter is **disabled** (`service.ts:669`, `catalog.ts:261–262`). | None. |
| 7.7 | **No retention deletion job** | implemented | `defaultAuditRetentionPolicies` are descriptive only. No code path calls `delete` on any audit repository based on retention. Retention metadata is exposed for *future enforcement* but not enforced. | Production-readiness item: retention enforcement (deletion + legal hold) is a planned v1 task. Not a blocker for v0 design conformance — the AGENTS.md rule 111 explicitly says retention policies must *not* delete data in v0. |

**Observability verdict**: `v0_implemented` (read-only, mock-first, no external export, no deletion).

---

## 8. Dashboard

| # | Surface | Status | Evidence | Concerns |
|---|---|---|---|---|
| 8.1 | **API-backed read model** | implemented | `apps/api/src/dashboard-read-model.ts` (910 LOC) builds DTOs from the existing per-domain repositories. 19 endpoints under `/dashboard/*` in `apps/api/src/main.ts`. `apps/web/lib/dashboard-data-provider.ts` provides `ApiDashboardDataProvider` that calls these endpoints. | None. |
| 8.2 | **Demo fallback explicit** | implemented | `AICHESTRA_DASHBOARD_DATA_SOURCE` env switch (`apps/web/lib/dashboard-data-provider.ts`); defaults to deterministic demo via `apps/web/lib/mock-data.ts`. Setting `AICHESTRA_DASHBOARD_DATA_SOURCE=api` + `AICHESTRA_DASHBOARD_API_BASE_URL=...` switches to API. `AICHESTRA_DASHBOARD_DISABLE_DEMO_FALLBACK=false` keeps an explicit fallback path when API is unreachable. | None. |
| 8.3 | **No workflow execution** | implemented | `/dashboard/*` endpoints only call repository read methods (`store.list*`, `service.list*`, etc.). No call to `runAgentTaskWorkflow`, no `gitService.createBranch`, no `llmGateway.completion`, no `agentRunnerService.runAgent`. | None. |
| 8.4 | **No provider calls** | implemented | The web app's `mock-data.ts:167–172` instantiates `OpenAICompatibleLLMProvider` *with no gates* — but only to capture the **blocked** completion result for the demo payload. The provider returns `blocked_remote_llm_disabled` and no HTTP call is made. (Same as in the prior current-state audit — worth a comment in code clarifying intent.) | Minor follow-up (carried over): add a 1-line comment near `apps/web/lib/mock-data.ts:167–172` explaining the intentional blocked-state capture. |
| 8.5 | **No secrets** | implemented | Sanitization is enforced at three places in the dashboard path: (a) `packages/shared/src/dashboard-read-models.ts:334–335` (`tokenLikePattern`, `credentialCachePattern`); (b) `apps/api/src/dashboard-read-model.ts` (e.g., `:431–432` exposes `remoteProviderPath: "openai_compatible"` and `selectedModel: config.defaultModel ?? "openai-compatible/default"` — never an API key); (c) `packages/observability/src/sanitizer.ts:17–18, 77` for audit payloads. Verified by `tests/dashboard-read-model-v0.test.ts` (no-secret assertions) and `tests/dashboard-data.test.ts`. | None. |
| 8.6 | **Sections cover Git / LLM / MCP / Auth / Policy / Security / Observability / Deployment** | implemented | 19 sections: `/dashboard/{overview,tasks,git,github-app,conflicts,registry,llm,agents,policy,auth,providers,security,local-agents,mcp,readiness,database,secret-backend,observability,audit}` (README.md lines 248–270; `apps/api/src/main.ts` routes). | None. |

**Dashboard verdict**: `v0_implemented` (API-backed, demo fallback, read-only).

---

## 9. Validation

| Command | Result | Detail |
|---|---|---|
| `pnpm lint` | **pass** | `scripts/lint.mjs` walked the tree — no trailing whitespace, no `fetch("http..."` matches, all JSON parses. |
| `pnpm typecheck` | **pass** | `tsc --noEmit -p tsconfig.typecheck.json` clean. |
| `pnpm build` | **pass** | All required design-doc / migration / source paths exist; `node --check` parses `apps/api/src/main.ts`, `apps/worker/src/main.ts`, `apps/web/src/main.ts`. |
| `git diff --check` | **pass** | Clean working tree, no whitespace errors. |
| `pnpm test` | **flaky** (1/3 runs failed, 2/3 runs green) | 228 tests / 224 pass / **0–1 fail** / 4 skipped. **Single flaky test**: `tests/local-agent-protocol-v1.test.ts:288` — `Enterprise Provider local_cli observes v1 channel, consent, compatibility, and fixture completion states` — fails inside the full suite with `actual: 'awaiting_consent'` vs `expected: 'provider_template_incompatible'`. **In isolation** (`node --test tests/local-agent-protocol-v1.test.ts`) the same test passes — 5/5 green, 0 fail. This is **the same flake** flagged in the current-state audit two iterations ago and confirms a test-isolation defect (cross-test state leakage in shared mock state, likely capability advertisements + invocation envelopes in `LocalAgentProtocolService`). |

**Test-suite stability summary across all three audits today**:
- Current-state audit (run 1): 1 fail (the flake)
- Current-state audit (re-run): 0 fail
- Current-state audit (re-run): 0 fail
- Phase-1/2 audit (run 1): 0 fail
- Phase-1/2 audit (re-run): 0 fail
- Phase-1/2 audit (re-run): 0 fail
- Integration foundations audit (run 1): 1 fail (the flake)
- Integration foundations audit (re-run): 0 fail
- Integration foundations audit (re-run): 0 fail
- Isolated `local-agent-protocol-v1.test.ts`: 5/5 green

Confirmed: **the only red signal in `pnpm test` is a known, intermittent test-isolation flake in `local-agent-protocol-v1.test.ts:288`**, with ~22% reproduction across 9 full-suite runs today.

---

## 10. Final Status

### Component statuses

| Track | Status | Headline |
|---|---|---|
| Persistent DB | `v1_implemented` (opt-in Postgres) + `v1_implemented` planning | StorageProvider abstraction + RepositoryFactory; Postgres opt-in via `AICHESTRA_STORAGE_PROVIDER=postgres`; migrations operator-invoked only; contract tests skip Postgres unless `AICHESTRA_TEST_DATABASE_URL` is set. |
| Real Git Adapter | `v2_implemented` (gated) | Mock default, Local fixture-only, GitHub gated at construction + every call; HMAC-SHA256 verifier with constant-time compare; SecretRef-first credentials; full audit/redaction. |
| LLM Gateway | `v2_implemented` (gated) | Mock + 1 gated OpenAI-compatible + 6 disabled skeletons + 1 Local CLI bridge; bounded fallback (disabled by default); virtual-key budget + policy + Auth + SecretRef integration; 0 real calls by default. |
| MCP Gateway | `v0_implemented` | Mock-only; real transport disabled; risk-level policy actions; secret/sandbox/network/write/deploy/local-execution all hard-denied at v0. |
| Local Agent Runner | `v1_implemented` | MockAgentRunner default; BlockedCommandExecutor default; FixtureLocalCommandExecutor with shell-meta blocking, hard deny list, no token env, byte/time caps; workspace allowlisted to tmpdir. |
| Local Agent Protocol | `v1_implemented` (mock-only) | `MockLocalAgentTransport` + `FixtureLocalAgentDaemon`; real transports denied by policy; vendor CLI skeletons only; **one known test-isolation flake**. |
| Policy-as-code | `v0_implemented` (deny-by-default) | `StaticPolicyEngine` returns `deny` when no rule matches; default rules deny remote git, remote LLM, secret reads, network egress, runner secret injection, MCP tools, Local Agent secret forwarding, improvement apply. |
| Auth/RBAC | `v0_implemented` (planning + MockAuthProvider) | All future providers throw; `MockAuthProvider` audits every resolve with `productionAuthEnabled: false`; feeds `PolicySubject`. |
| Secrets/Sandbox | `v0_implemented` (metadata-only) | `SecretRef` has no `value` field; sandbox kinds are future placeholders; network egress denied by default. |
| SecretRef-backed Provider Credentials | `v1_implemented` | AuthZ → policy → lease → env access path; raw secret rejected at API; legacy env fallback audited. |
| Observability / Audit Retention | `v0_implemented` (read-only) | Common audit envelope, retention/redaction classes, 14 audit-source normalization records, metric snapshot, trace skeleton, no external exporter, no deletion job. |
| Dashboard | `v0_implemented` (API-backed) | 19 read-only sections; explicit demo fallback; no workflow / provider calls; three-layer secret sanitization. |

### Critical blockers

**None.** Default runtime makes zero external calls; every integration boundary is gated; no production-readiness claim is made anywhere in the repo.

### Important follow-ups

1. **Fix the `local-agent-protocol-v1.test.ts:288` test-isolation flake.** Reproduced ~22% across 9 full-suite runs today; passes 5/5 in isolation. Likely cross-test contamination of `LocalAgentProtocolService` repositories (capability advertisements + invocation envelopes) when tests share process state. Suggested approach: (a) trace which prior test sets stable agent IDs that collide; (b) move `createProtocol()` into `beforeEach` if not already isolated per-test; (c) run `node --test --shuffle --concurrency=1` to surface the contamination deterministically.
2. **Implement runtime webhook delivery dedupe + replay-rejection** in `packages/git-adapter/src/webhooks.ts` and `packages/db/src/repository.ts:398–406`. Currently planning-only in `packages/deployment-readiness`. Add: idempotent insertion keyed by `deliveryId`; `same-deliveryId-different-payloadHash` → `replay_rejected` + audit event; dead-letter status on processing failure; two tests for duplicate-ignored and replay-rejected.
3. **Negative-path test for SecretRef vs. legacy env fallback.** Carried over from the SecretRef v1 completion review and Phase-1/2 audit: assert `disabled`/`revoked` `_SECRET_REF` does not silently fall back to `AICHESTRA_GITHUB_TOKEN` / `AICHESTRA_GITHUB_WEBHOOK_SECRET` / `AICHESTRA_LLM_API_KEY`.
4. **Consolidate worker on `StaticPolicyEngine`.** The worker workflow (`apps/worker/src/workflows/run-agent-task-workflow.ts:137–155`) still uses the legacy `MockPolicyEngine` (`LegacyTaskPolicyEngine`); the rest of the API uses the modern `PolicyService` + `StaticPolicyEngine`. Single deny-by-default codepath would close one drift surface.
5. **Tighten `scripts/lint.mjs:37–39`** to also catch `fetch(\`https://...\`)` and `fetch(new URL("https://..."))` template-literal/URL forms. Defense-in-depth — the current grep relies on the two known sites staying behind gates.
6. **Document fresh-clone workflow note.** On a freshly cloned tree, `pnpm typecheck` initially fails when a new workspace package is added (today: `@aichestra/observability`). Either auto-run `pnpm install` from CI or note in `CONTRIBUTING.md`.
7. **Annotate `apps/web/lib/mock-data.ts:167–172`.** Add a one-line comment explaining that `new OpenAICompatibleLLMProvider()` is intentionally constructed without gates to capture the blocked-state payload for the dashboard demo. Future readers may misread this as a regression.
8. **Production-readiness tracks unchanged** (Auth/RBAC v1 with real IdP, durable observability backend, retention deletion + legal hold, secret-backend implementation, GitHub App private-key handling, K8s/cloud deploy, tenant isolation). All explicitly tracked in `docs/roadmaps/*` and gated by AGENTS.md.

### Recommended next task

**Fix the test-isolation flake in `tests/local-agent-protocol-v1.test.ts:288` first** (Follow-up #1). It is the only red signal in the entire validation matrix across the three audits I've performed today; once green and stable, the validation pipeline becomes a clean signal for every downstream change. After that, **implement webhook delivery dedupe + replay-rejection** (Follow-up #2), which moves GitHub App / Production Webhook Hardening from `planning_only` to `v1_implemented` without touching any other phase.
