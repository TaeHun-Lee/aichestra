# Runtime Component Inventory

Production Deployment Readiness Planning v0 inventory. Status values:

- `ready`: production-ready within current scope.
- `partial`: useful boundary exists, but production work remains.
- `not_ready`: implemented for local/integration use only.
- `future`: planned boundary only.

No component in this inventory should be read as approving production deployment.

| Component | Purpose | Current runtime status | Deployment unit | State | Required env vars | Network access | Required secrets | Persistent storage | Health/readiness needs | Scaling notes | Production readiness |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `apps/api` | REST control plane for tasks, registries, Git, LLM, runner, auth, security, MCP, dashboard, and readiness read models. | Node HTTP server, mock-first defaults. | api | stateless if repositories are externalized | `AICHESTRA_STORAGE_PROVIDER`, `AICHESTRA_DATABASE_URL` when Postgres; provider gates as configured | Local only by default; gated Git/LLM HTTP only through provider boundaries | SecretRef ids only; no raw secrets in responses | In-memory default, optional Postgres core slice | `/health`, dependency health, production profile validator future, auth readiness metadata | Horizontal API replicas require durable repositories and shared auth/session model | partial |
| `apps/web` | Dashboard rendering and API-backed read-model consumption. | Static/demo fallback plus API mode. | web | stateless | `AICHESTRA_DASHBOARD_DATA_SOURCE`, `AICHESTRA_DASHBOARD_API_BASE_URL`, `AICHESTRA_DASHBOARD_DISABLE_DEMO_FALLBACK` | API only in API mode | none | none | API connectivity, no demo fallback in production | Horizontally scalable after auth/session model exists | partial |
| Worker/task orchestration path | Runs MVP task workflow with mock branch, runner, PR, usage, merge queue. | Synchronous/local workflow path. | worker | stateless after durable stores exist | Same storage/provider/runner gates as API | No external calls by default | Uses gateway boundaries only | Core task/run/registry state needs Postgres | Worker queue health, idempotency, retry, dead-letter future | Needs queue and concurrency controls before production | partial |
| Database/Postgres | Durable storage for core slice. | Opt-in Postgres v1 plus in-memory default; production operations v1 read models exist. | api, worker, migration-job | stateful | `AICHESTRA_STORAGE_PROVIDER=postgres`, `AICHESTRA_DATABASE_URL` | DB network only when Postgres provider is explicitly selected | DB credentials in URL or future SecretRef | Postgres for core slice; many stores still future | Pool health, migration state, lag, backup status; v1 exposes safe booleans/counts only | Needs pooling, backup/restore, migration governance, retention/legal hold, and read/write capacity planning | partial |
| Migration runner | Explicit SQL migration execution. | `pnpm db:migrate` only. | migration-job | stateless | `AICHESTRA_DATABASE_URL`, optional `AICHESTRA_PSQL_BIN` | DB network only | DB credentials | Migration history future | Pre/post checks, lock, rollback plan | Run once per release with backup gate | partial |
| Git adapter | Mock/local Git, gated GitHub branch/PR/webhook/sync, and Branch Orchestrator v2 metadata. | Mock default; GitHub gated; webhook disabled by default; GitHub App Controlled Implementation v1 adds mock/status-only app token-handle boundary; branch orchestration is metadata-only. | api, worker | stateful in-memory branch orchestration records until durable repositories exist | `AICHESTRA_GIT_PROVIDER`, remote gates, allowlists, webhook gates, optional GitHub App auth-mode gates | GitHub only through gated `GitHubClient`; no GitHub App HTTP call in default runtime/tests; Branch Orchestrator v2 performs no Git calls | `AICHESTRA_GITHUB_TOKEN_SECRET_REF` preferred for legacy mode; `AICHESTRA_GITHUB_APP_PRIVATE_KEY_SECRET_REF` metadata for GitHub App mode; legacy env fallback local/integration only | Repos/PRs/webhook/sync stores need durable coverage; branch orchestration needs future durable request/decision/ownership/drift/audit tables; GitHub App runtime state is derived from config in v1 | Provider config, webhook verifier, GitHub App token provider status, branch ownership summary, collision blockers, base drift metadata, rate-limit future | Scale with idempotent webhook processing, queue, and durable branch ownership records | partial |
| Multi-user / Multi-session Branch Orchestrator | Metadata-only branch allocation and ownership coordination for concurrent users/sessions against the same repo. | v2 implemented; in-memory only; deterministic `aichestra/` naming, safe prefix validation, branch collision detection, same-workspace blockers, BranchLease/WorkspaceLease linkage, base drift metadata, and no-destructive-Git status. | api, web | stateful in-memory orchestration records | none | none | none | Future durable `branch_orchestration_requests`, `branch_orchestration_decisions`, `branch_ownership_records`, `base_branch_drift_statuses`, naming policies, and audit tables before production concurrency | `/git/branches/orchestrate`, `/git/branches/orchestration*`, `/git/branches/ownership`, `/git/branches/drift`, `/dashboard/git` | Needs durable store, distributed ownership locking, real drift calculation behind safe gates, production merge governance, and tenant scoping before production branch orchestration | partial |
| Merge Queue Policy | Mock-first policy-driven merge readiness decisions for existing merge queue entries. | v2 implemented; metadata-only decisions, holds, deterministic ranking, Policy-as-code gates, dashboard/readiness visibility; merge execution and auto-merge disabled. | api, web | stateful in-memory decisions/holds over existing queue entries | none | none | none | Future durable `merge_readiness_decisions`, `merge_queue_holds`, and policy revision tables before production merge governance | `/git/merge-queue/*`, `/dashboard/conflicts`, disabled merge execution and no-secret/no-env status | Needs durable decision history, human review workflow, conflict resolution assistant, live integration-test profile, and separately approved future merge execution design | partial |
| LLM Gateway | Model routing, budget, usage, mock completions, gated OpenAI-compatible path. | Mock default; OpenAI-compatible remote path gated. | api, worker | stateless with durable usage/audit | LLM provider/routing/fallback/env gates | Remote LLM only when all gates pass | `AICHESTRA_LLM_API_KEY_SECRET_REF` preferred | Usage ledger durable; routes/audit mostly in-memory | Provider health, budget, route decisions | Needs queue/rate limit and provider quotas | partial |
| Local CLI Provider Templates | Metadata-only templates for Claude Code, OpenAI Codex CLI, Gemini CLI, Aider, and custom local CLI providers, plus compatibility rules, parser profiles, security constraints, and readiness. | v1 implemented; template-only, disabled-by-default, Local Agent required, no direct execution, no PTY, no credential-cache read, no secret forwarding, no env value exposure. | api, web | deterministic seed/read model | none | none | none; external CLI session metadata only | derived readiness state only | `/providers/local-cli/templates`, `/providers/local-cli/compatibility`, `/providers/local-cli/security-constraints`, `/providers/local-cli/readiness`, `/dashboard/providers` | Needs skipped-by-default integration-test profile and real Local Agent product/security design before live vendor CLI validation | partial |
| LLM Gateway integration-test readiness | Optional OpenAI-compatible live-test profile, test cases, safety checks, model allowlist, budget cap, SecretRef preference, prompt/output redaction, and summary metadata. | Read-only v1; skipped by default; no provider calls, API key reads, base URL value exposure, streaming, tool calling, vendor CLI, or credential cache access in default runtime/tests. | api | deterministic seed/read model plus safe env booleans/counts | LLM integration-test env gates only when live tests are explicitly reviewed | none by default; optional live tests only when all gates pass | SecretRef id preferred; raw test env key allowed only as controlled test metadata | derived readiness state only | `/readiness/llm-integration/*`, `/dashboard/llm-integration`, `/health` LLM integration metadata, no-secret/no-env/no-raw-response status | Needs production secret backend, active integration CI profile, provider quota policy, durable audit/usage, and tenant scoping before broader live testing | partial |
| Vault integration-test readiness | Optional Vault KV v2 live-test profile, required gates, test-only SecretRef pattern, path allowlist checks, no-write/no-delete/no-rotate/no-broad-list policy, safety checks, and summary metadata. | Read-only v1; skipped by default; no Vault calls, token reads, address/path/key/value exposure, broad path listing, writes, deletes, rotations, or credential cache access in default runtime/tests. | api | deterministic seed/read model plus safe env booleans/counts | Vault integration-test env gates only when live tests are explicitly reviewed | none by default; optional Vault live tests only when all gates pass | SecretRef metadata only; no raw token/path/key/value returned | derived readiness state only | `/readiness/vault-integration/*`, `/dashboard/vault-integration`, `/health` Vault integration metadata, no-secret/no-env/no-token status | Needs operator-managed non-production Vault test path, production Vault rollout, rotation policy, production auth, policy bundle runtime, and tenant scoping before broader live validation | partial |
| MCP Gateway | Mock-first tool governance and deterministic low-risk mock invocation. | Mock default; real transport disabled. | api | stateful in-memory in v0 | future MCP env vars only | none in v0 | SecretRefs modeled but not issued | In-memory MCP catalog/invocations/audit | Gateway config, tool risk summary | Needs durable catalog and real transport governance later | partial |
| Local Agent Protocol service | Future user-machine Local Agent coordination boundary. | Mock/fixture-only protocol. | api, future local-agent | stateful in-memory | none for real transport in v1 | none; mock transport only | no secret forwarding | In-memory protocol stores | Agent/channel/consent/stream health | Needs production transport, pairing, tenant scoping | partial |
| Local Agent Runner | Mock runner, disabled local fixture command runner, Agent Workspace Lifecycle v2 metadata, Multi-session Agent Run Coordination v1 metadata, and Cross-session File Lease / Edit Intent Graph v1 metadata. | Mock default; command execution disabled; per-AgentRun workspace leases are in-memory metadata; session coordination groups/overlaps are in-memory metadata; file leases/edit intents/graphs are in-memory metadata; future Git worktree execution disabled; Merge Queue Policy v2 consumes runner metadata as readiness evidence only. | worker | stateful in-memory run, workspace lifecycle, coordination, and edit-intent records | `AICHESTRA_AGENT_RUNNER`, local runner gates, workspace root | no network by default | no secrets injected | In-memory runner/workspace/command records plus workspace leases/events/cleanup decisions, coordination sessions/overlaps/audit, and edit intents/file leases/overlap assessments | Executor kind, workspace status, workspace lifecycle status, cleanup decision status, command limits, coordination summary, edit-intent summary | Needs production sandbox, durable runner/coordination/edit-intent stores, distributed locks, and explicit future worktree allocator before production workspace use | partial |
| Multi-session Agent Run Coordination | Metadata-only coordination for concurrent agent sessions against the same repo/base branch/task/user/source scope. | v1 implemented; in-memory only; detects same workspace, same branch, same file, same directory, missing target files, and base branch drift; can cross-reference Branch Orchestrator v2 branch ownership metadata; Merge Queue Policy v2 consumes overlaps for readiness holds; no execution, Git mutation, provider calls, workspace deletion, or secret exposure. | api, worker | stateful in-memory coordination records | none | none | none | Future durable `agent_sessions`, `agent_run_coordination_groups`, `agent_session_overlaps`, `agent_concurrency_policies`, and audit tables before production concurrency | `/agents/sessions`, `/agents/coordination/*`, `/dashboard/agents`, no-destructive-action and no-secret status | Needs durable store, distributed locking, branch ownership durability, edit-intent/file leases, production merge governance, and tenant scoping before production concurrency control | partial |
| Cross-session File Lease / Edit Intent Graph | Metadata-only file lease and edit intent graph for concurrent agent sessions targeting the same repo/files/directories. | v1 implemented; in-memory only; detects same-file write intent, read/write overlap, same-directory overlap, broad refactor overlap, same-branch overlap, same-workspace blockers, and broad unknown target warnings; Merge Queue Policy v2 consumes overlap assessments for readiness holds; no OS file locks, source mutation, Git mutation, provider calls, workspace deletion, or secret exposure. | api, worker | stateful in-memory edit-intent records | none | none | none | Future durable `file_leases`, `edit_intents`, `edit_intent_graph_nodes`, `edit_intent_graph_edges`, and `edit_overlap_assessments` tables before production concurrency | `/agents/edit-intents`, `/agents/file-leases`, `/agents/edit-intent-graph`, `/agents/edit-overlaps`, `/agents/edit-intent-summary`, `/dashboard/agents`, no-file-lock/no-source-mutation status | Needs durable store, distributed reservations, symbol-aware analysis, production merge governance, and tenant scoping before production concurrency control | partial |
| Registry services | Skill/Harness/Instruction registries and governance metadata. | Implemented with in-memory/file/Postgres core registry slice. | api, worker | stateful | storage env for persistence | none | none | Postgres for registry core; improvement governance in-memory | Registry health, approval/eval status | Needs artifact signing/registry before production | partial |
| Policy engine | Static deny-by-default policy decisions and audit. | Static TypeScript rules. | api, worker | in-memory audit | none | none | none | In-memory audit, future policy tables | Rule version and decision audit | Needs signed/versioned bundle source | partial |
| Policy bundle readiness | Planning models for engine options, bundle plans, domain mappings, review workflow, tests, rollout/rollback, break-glass, risks, and migration phases. | Read-only v0 planning; `StaticPolicyEngine` remains runtime; no OPA/Rego, Cedar, external policy service, dynamic execution, remote bundle loading, hot reload, or signing verification. | api | deterministic seed/read model | none | none | none | derived planning state | `/readiness/policy-bundles/*`, `/dashboard/policy-bundles`, `/health` policy bundle metadata, no-dynamic-execution status | Needs schema validator, review workflow, signed bundle verification, shadow evaluation, rollout/rollback, production Auth/RBAC, tenant scoping, and audit export before runtime use | partial |
| Policy shadow evaluation readiness | Planning models for comparing future candidate runtime decisions against `StaticPolicyEngine`, including comparison rules, mismatch taxonomy, reports, checks, rollout, and dashboard metadata. | Read-only v1 planning; no shadow evaluator, no candidate runtime execution, no OPA/Rego or Cedar runtime, no signed bundle verification runtime, no dynamic policy execution, no external policy service, and no enforcement change. | api, web | deterministic seed/read model | none | none | none | derived planning state only | `/readiness/policy-shadow/*`, `/dashboard/policy-shadow`, `/health` policy shadow metadata, no-dynamic-execution and no-secret/no-env status | Needs disabled skeleton, offline golden comparison, durable audit/metrics, tenant scoping, signed bundle verification, and explicit rollout controls before live shadow | partial |
| Policy runtime PoC readiness | Planning models for future policy runtime PoC options, input/output contract, domain mappings, golden cases, shadow evaluation planning, comparison rules, mismatch taxonomy, reporting, rollout/rollback, risks, summary, and offline golden harness status. | Read-only v0 planning plus Golden Harness v1 static comparison plus Shadow Evaluation Planning v1 metadata; `StaticPolicyEngine` remains source of truth; no OPA/Rego, Cedar, signed JSON/YAML evaluator, signed bundle verification, custom policy service, shadow evaluator, candidate runtime, dynamic execution, remote loading, hot reload, or runtime enforcement. | api | deterministic seed/read model plus static fixtures | none | none | none | derived planning state | `/readiness/policy-runtime-poc/*`, `/readiness/policy-runtime-poc/golden-summary`, `/readiness/policy-shadow/*`, `/dashboard/policy-runtime-poc`, `/health` policy runtime PoC metadata, no-runtime-execution status | Needs shadow evaluator skeleton, mismatch audit implementation, rollout/rollback controls, production Auth/RBAC, tenant scoping, and signed bundle verification before runtime use | partial |
| Auth/RBAC mock provider | Provider-neutral auth/RBAC planning boundary. | Mock provider default; future providers disabled. | api | in-memory | future auth env vars | none | no tokens/sessions | In-memory auth repos/audit | Auth mode, role/permission counts | Needs real IdP, session, tenant storage | not_ready |
| Production Auth/RBAC readiness | Planning models for IdP options, migration phases, readiness checks, risks, tenant boundaries, service accounts, and production permission matrix. | Read-only v1 planning; no real OIDC/SAML/SCIM/SSO/login/session/JWT/password/provider call behavior. | api | deterministic seed/read model | none | none | none; tokens/cookies/session ids/assertions are not read or stored | derived planning state | `/readiness/auth/*`, `/dashboard/auth-production`, `/health` auth readiness metadata, no-token/no-session status | Needs real AuthProvider, durable auth repositories, tenant scoping, service-account credential issuance, session model, SCIM sync, and break-glass workflow | partial |
| Production auth provider skeleton | Disabled future provider boundary and safe provider-selection/readiness metadata for production auth. | Read-only v1 skeleton; `MockAuthProvider` remains active default; future OIDC/SAML/SCIM/vendor/custom providers fail closed; no token validation, cookie parsing, session issuance, SCIM sync, external IdP calls, or production-auth enablement. | api, web | deterministic seed/read model plus safe config booleans | `AICHESTRA_AUTH_PROVIDER`, `AICHESTRA_ENABLE_PRODUCTION_AUTH`, `AICHESTRA_REQUIRE_AUTH_FOR_API`, future OIDC/SAML/SCIM config names | none | none; tokens/cookies/session ids/raw claims/env values are not read or stored | derived planning state only | `/readiness/auth-providers/*`, `/dashboard/auth-providers`, `/auth/config`, `/health` auth provider skeleton metadata, no-token/no-session/no-env status | Needs real provider hardening, token validator design, session storage, SCIM sync design, durable identity mappings, tenant scoping, and break-glass workflow before production auth | partial |
| SecretRef/CredentialManager | Metadata-only credential resolution with explicit env and gated Vault providers. | SecretRef v1 implemented; Vault-backed Secret Backend v1 implemented as a non-default gated boundary; cloud backends future. | api, worker | in-memory | `AICHESTRA_ENABLE_ENV_SECRET_PROVIDER`, `AICHESTRA_ALLOWED_SECRET_ENV_KEYS`, optional Vault gates | Vault only through `GatedHttpVaultClient` when explicitly configured; none by default | SecretRef ids; transient env/Vault value only inside adapter boundary | In-memory security repos | Credential status, audit, redaction, Vault config/health/readiness, secret backend readiness | Needs production Vault rollout, rotation/revocation, production auth, policy bundle runtime, and env fallback enforcement | partial |
| Secrets/Sandbox service | Secret metadata, leases, sandbox profiles, network/redaction policies. | Metadata-only; no real sandbox/network enforcement. | api, worker | in-memory | none required for metadata | none | no raw secrets | In-memory security repos | Secret/sandbox/network/redaction config | Needs production sandbox and network controls | partial |
| Dashboard read model composer | Read-only API aggregation for dashboard sections. | API-backed v0 with demo fallback. | api | derived | dashboard source env for web | none beyond API | none | derived from source repos | Read-only route health and no-secret sanitization | Needs tenant/RBAC scoping | partial |
| Dashboard/readiness tenant scope planning and implementation | Inventory, metadata, and UI readiness surface for future dashboard/readiness tenant, team, project, repo, provider, model, SecretRef, MCP, registry, local-agent, and audit-query filtering. | Read-only v1 planning plus metadata implementation; deterministic models; no production tenant enforcement, no production Auth/RBAC, no row-level security, no external calls. | api, web | derived static planning metadata | none | none | none | none | `/readiness/tenant-scope/*`, `/readiness/scopes/*`, representative `/readiness/*/summary`, `/dashboard/tenant-scope`, `/health` planning metadata, scope summaries, missing-scope warnings, no-secret/no-env status | Needs future implementation for request scope extraction, dashboard/readiness filters, audit query filters, repository filters, cache partitioning, and production Auth/RBAC | partial |
| Tenant scope enforcement helper | Partial representative scope comparison, enforcement-mode metadata, mismatch catalog, dashboard/readiness warnings, audit-query warnings, and secret-adjacent helper decisions. | v1_implemented_partial; helper/service metadata only; no production tenant filtering, no production Auth/RBAC, no row-level security, no DB tenant partitioning, no external calls. | api, web | derived from request/read-model metadata | none | none | none | none | `/readiness/tenant-enforcement/*`, `/dashboard/tenant-enforcement`, `scopeDecisionSummary`, `/health` tenant scope enforcement metadata, no-secret/no-env status | Needs trusted production AuthContext scopes, durable grants, repository/query filters, cache partitioning, audit query filters, and policy shadow evaluation before production enforcement | partial |
| Observability read model service | Common audit envelope, retention/redaction metadata, source coverage, metric snapshot, and trace skeletons. | In-memory/read-only v0; no external backend, exporter, alert delivery, or retention deletion. | api | derived from current audit repositories | none | none | none | derived; no durable common audit table yet | `/observability/config`, `/observability/audit/summary`, source coverage, no-secret status | Needs durable audit storage, retention jobs, export, metrics/traces backend, and tenant scoping | partial |
| GitHub App / webhook hardening readiness | Planning models for GitHub App descriptor/installations/repo grants, permission matrix, webhook allowlist, replay/dead-letter, credential readiness, endpoint plan, and risks. | Read-only planning v0 plus local receiver duplicate/replay classification; GitHub App Controlled Implementation v1 adds runtime/read-model config, installation, repo grant, and mock token-handle checks; no live GitHub App or production webhook behavior. | api | deterministic seed/read model plus config-derived runtime state | GitHub App env gates when configured | none by default; no live GitHub App HTTP call in v1 | SecretRef ids only; private keys/webhook secrets/tokens are not read or stored | derived planning state plus local webhook event store; future durable app tables only if live profile proceeds | `/readiness/github-app/*`, `/git/github-app/*`, `/dashboard/github-app`, `/health` GitHub App metadata, no-secret status | Needs real secret backend, private-key signing boundary, installation token exchange, durable replay store, queue/dead-letter worker, production endpoint, alerting, and tenant scoping | partial |
| Persistent DB operations readiness | Planning models for DB deployment profiles, migration metadata, index review, retention/audit growth, webhook persistence, backup/restore, pooling, checks, risks, and summaries. | Read-only v1; no production DB connection, migration execution, backup/restore job, retention deletion, or partition job. | api | deterministic seed/read model plus local migration file metadata | none required; reads env names as booleans only | none | none; DB URL values are not exposed | derived planning state and local migration files | `/readiness/database/*`, `/dashboard/database`, `/health` DB operations metadata, no-DB-url status | Needs pooled DB client, release migration job, backups, restore drills, retention/legal hold, durable replay/dead-letter persistence, and tenant scoping | partial |
| Secret backend migration readiness | Planning models for backend options, SecretRef migration phases, readiness checks, risks, rotation plans, lease policies, env fallback posture, and summaries. | Read-only planning v0 plus Vault v1 implementation reference; no secret migration, rotation job, credential issuance, cache read, or env value exposure. | api | deterministic seed/read model | none | none | none; SecretRef ids/counts/status only | derived planning state | `/readiness/secrets/*`, `/dashboard/secret-backend`, `/health` secret backend metadata, no-secret/no-env status | Needs production Vault rollout, production Auth/RBAC, service account identity, policy bundle governance, rotation/revocation workflow, backend audit correlation, and production env fallback enforcement | partial |
| Production secret backend option decision readiness | Decision criteria, backend scores, Vault-first recommendation, provider mapping, v1 implementation scope, migration strategy, test strategy, and risk register. | Read-only v0; selected Vault for v1 and AWS Secrets Manager as second choice. Vault v1 is now implemented as a gated boundary, not a production backend rollout. | api | deterministic seed/read model | none required; future gates are names/status only | none | none; no backend refs or secret values | derived planning state | `/readiness/secret-backend-decision/*`, `/dashboard/secret-backend-decision`, `/health` secret backend decision metadata, no-secret/no-env status | Needs optional gated live test profile, production auth, policy bundle runtime, operational runbooks, and env fallback enforcement | partial |
| Vault secret backend | Gated Vault SecretRef provider, Vault client boundary, metadata-only health/readiness/dashboard status, and mock/live-test client split. | v1 implemented; disabled by default; mock in deterministic tests; gated HTTP client only with explicit Vault gates; not production-ready. | api, worker | in-memory security repos plus external Vault only when explicitly configured | `AICHESTRA_SECRET_BACKEND_PROVIDER=vault`, `AICHESTRA_ENABLE_VAULT_SECRET_PROVIDER`, `AICHESTRA_VAULT_ADDR`, `AICHESTRA_VAULT_AUTH_METHOD`, `AICHESTRA_VAULT_TOKEN` for gated integration tests, `AICHESTRA_VAULT_ALLOWED_PATH_PREFIXES` | none by default; Vault HTTP only in gated path | SecretRef ids and metadata; no raw secrets in DTOs | In-memory credential/audit/lease metadata | `/readiness/secrets/vault/*`, `/security/secrets/vault/*`, `/dashboard/vault-secret-backend`, `/health` Vault metadata, no-secret/no-token status | Needs production Vault deployment, service identity, HA/unseal ownership, audit devices, rotation, migration, break-glass, and live profile validation | partial |
| Staging deployment profile readiness | Non-production staging profile, integration gates, readiness checks, promotion criteria, rollback criteria, risk register, and gate matrix. | Read-only v0; no deployment, infrastructure manifests, external provider calls, production traffic, remote MCP, vendor CLI execution, or secret/env value exposure. | api | deterministic seed/read model | none required; reports env names as policy only | none | none | derived planning state | `/readiness/staging/*`, `/dashboard/staging`, `/health` staging metadata, no-secret/no-env status | Needs actual staging deployment, CI/CD pipeline, real secret backend, production Auth/RBAC, Postgres profile, backup/restore evidence, and optional integration-test profiles | partial |
| Staging deployment dry-run readiness | Aggregated dry-run profile, sources, checks, blockers, integration profile classifications, report, summary, promotion guidance, and rollback guidance. | Read-only v0; no deployment, CI job execution, remote integration-test execution, external provider calls, resource mutation, destructive Git, real MCP transport, vendor CLI execution, or secret/env value exposure. | api | deterministic seed/read model | none required; reports env names and statuses only | none | none | derived planning state | `/readiness/staging-dry-run/*`, `/dashboard/staging-dry-run`, `/health` staging dry-run metadata, no-secret/no-env status | Needs release-candidate checklist, accepted staging fallback decisions, real secret backend decision, and operational staging evidence before live staging validation | partial |
| Staging release candidate readiness | Read-only RC checklist, validation gates, optional skipped integration policy, blockers, signoffs, release-note requirements, rollback checklist, report, summary, and recommended next actions. | Read-only v0; no release creation, Git tag creation, GitHub release creation, deployment, CI job execution, remote integration-test execution, external provider calls, resource mutation, destructive Git, real MCP transport, vendor CLI execution, or secret/env value exposure. | api | deterministic seed/read model | none required; reports env names and statuses only | none | none | derived planning state | `/readiness/staging-rc/*`, `/dashboard/staging-rc`, `/health` staging RC metadata, no-release/no-deployment/no-secret/no-env status | Needs validation evidence, release notes, signoff records, and staging RC audit before a branch is called a release candidate | partial |
| Staging deployment execution readiness | Read-only controlled staging execution sequence, pre-deploy gates, manual signoff requirements, human signoff pack metadata, optional live integration decisions, go/no-go model, post-deploy smoke placeholders, and rollback plan. | Read-only v0; no deployment, release creation, Git tag creation, deployment command execution, CI job execution, remote integration-test execution, external provider call, resource mutation, credential issuance, real approval recording, or secret/env value exposure. | api | deterministic seed/read model | none required; reports env names and statuses only | none | none | derived planning state | `/readiness/staging-execution/*`, `/dashboard/staging-execution`, `/health` staging execution metadata, signoff pack pending metadata, no-deployment/no-secret/no-env status | Needs real human signoffs, Staging Deployment Approval Audit v0, accepted integration decisions, and an explicitly approved deployment task before live staging deployment | partial |
| Staging CI/CD pipeline readiness | Pipeline profiles, job matrix, optional integration-test gates, secret/env safety, artifact/report policy, staging promotion criteria, cleanup/rollback policy, checks, and risks. | Read-only v0; no active workflow creation, deployment, CI job execution, external provider calls, default remote integration tests, artifact upload, or secret/env value exposure. | api | deterministic seed/read model | none required; reports env names as policy only | none | none | derived planning state | `/readiness/ci-cd/*`, `/dashboard/ci-cd`, `/health` CI/CD metadata, no-secret/no-env status | Needs active CI workflow implementation, protected integration-test profiles, artifact/report redaction enforcement, controlled cleanup jobs, and staging dry-run evidence before live staging promotion | partial |
| Future Local Agent daemon | User-machine daemon for local CLI/provider coordination. | Not implemented. | local-agent | stateful local process future | future agent registration/env | future controlled outbound/inbound transport | must never read vendor caches without explicit future design | local metadata future | Pairing, update, revocation, consent | Requires separate product/security design | future |
| Future webhook receiver boundary | Production webhook endpoint hardening. | GitHub webhook v2 safe receiver exists with local duplicate/replay checks; production hardening incomplete. | api, background-job | local store now, future durable idempotency | GitHub webhook gates, future GitHub App SecretRefs | GitHub inbound HTTP | webhook SecretRef preferred; future GitHub App private key SecretRef for token exchange, not used in v0 | webhook event/audit/sync tables plus future delivery/replay/dead-letter tables | signature, replay, idempotency, rate limit, dead-letter, no-secret status | Needs queue, durable replay handling, production endpoint rollout, and alerting | partial |
| Future background worker/queue | Async workflow, webhook, audit export, retry processing. | Not implemented. | background-job | stateful queue | future queue env vars | queue network | queue credentials via SecretRef future | queue/dlq storage | lag, failures, retry/dead letter | Required before production scale | future |
| Future observability collector | Logs, metrics, traces, audit export, alerting, and retention enforcement. | Not implemented; v0 only has read-model skeletons. | future | stateful backend future | future observability env vars | collector/export endpoints | backend credentials via SecretRef future | observability backend | ingestion, retention, alert health | Required before production | future |

## OIDC Provider Skeleton Hardening

Status: `v1_implemented`. Component kind: disabled future auth-provider skeleton. Runtime default remains MockAuthProvider. External calls, discovery/JWKS fetches, JWT validation, sessions, API keys, service-account credential issuance, and secret/env exposure are disabled.
