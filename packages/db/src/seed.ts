import { seedHarnesses, seedInstructions, seedRepos, seedSkills, seedUsers } from "@aichestra/core";

export const seedData = {
  users: seedUsers,
  repos: seedRepos,
  skills: seedSkills,
  harnesses: seedHarnesses,
  instructions: seedInstructions
};
