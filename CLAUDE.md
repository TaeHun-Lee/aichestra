# CLAUDE.md

This repository uses `AGENTS.md` as the canonical project guidance for AI coding agents.

Read `AGENTS.md` first, then consult:

- `docs/ARCHITECTURE.md`
- `docs/MERGE_ALGORITHM.md`
- `docs/SEMANTIC_MERGE.md`
- `.aichestra/config.yaml`

Key invariant: never weaken the verified-tree rule. The exact tree tested in preflight must be the tree applied to main.
